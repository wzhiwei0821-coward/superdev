---
title: mpdev v2.1.1 — install/publish 双源（GitHub + GitLab 内网）
status: approved
date: 2026-05-18
author: brainstorming session
target_version: 2.1.1
breaking_change: false
---

# mpdev v2.1.1 — install/publish 双源支持

## 1. 问题陈述

mpdev v2.x 当前仅支持 GitHub 公网作为安装源。但本套件主要用户是**内网团队**（10.173.28.211 GitLab），他们：

1. 装 plugin 时优先内网拉，GitHub 走公网慢/被墙
2. 维护者发布新版时，希望**同时**推到 GitHub 公网（社区可见） + GitLab 内网（团队用）
3. GitLab 仓 `http://10.173.28.211/robot-ai/mppm/mpdev` **已存在**，结构为独立 mpdev 仓（仓根即 plugin 内容，无 monorepo subdir），分支 `master`

参考 mppm-master 的 `bin/install.sh` 已用 `--source=github|internal` 模式实现内外网双源。我们做对应的 mpdev 版本。

## 2. 设计目标

- **install (拉)**：用户跑 `install.sh` 默认从 GitLab 内网（SSH）装；`--source=github` 切公网（HTTPS）
- **publish (推)**：维护者跑 `scripts/sync-to-gitlab.sh` 一键把 `superdev/mpdev/` 同步到 GitLab 独立仓
- **零破坏**：v2.1.0 用户的安装 `~/dev/mpdev/` 保留原 origin，`/plugin update` 路径不变
- **明确划界**：sync 脚本仅供维护者用；最终用户**不接触** sync 逻辑

成功标准：
- 内网用户跑 `bash install.sh` 直接从 GitLab 装成功（前提：SSH key 已配置）
- 外网用户跑 `bash install.sh --source=github` 从 GitHub 装成功
- 维护者在 `superdev/` 根跑 `bash mpdev/scripts/sync-to-gitlab.sh` 后，GitLab `mpdev` 仓含最新 mpdev/ 内容 + `vX.Y.Z` tag
- `--help` 显示完整 source / target / branch / subdir 用法
- SSH 鉴权失败时给清晰修复指引（添 key / VPN / ssh-keyscan）

## 3. 决策记录

| 决策 | 选择 | 理由 |
|------|------|------|
| **D1 GitLab 仓结构** | 独立仓（仓根 = plugin，无 SUBDIR） | URL 结构 `robot-ai/mppm/mpdev` 暗示 mpdev 是与 mppm 同级的独立 project；clone 出来直接是 plugin |
| **D2 GitLab 鉴权** | SSH（`git@10.173.28.211:...`） | 跟随 mppm 同模式；公司 GitLab 内网用 SSH key 是标准做法 |
| **D3 GitLab 分支** | `master` | URL `/tree/master` 已明示；与 mppm 一致 |
| **D4 默认源** | gitlab | 用户群以内网团队为主；外部用户加 `--source=github` flag 切换 |
| **D5 切换机制** | `--source=gitlab|github` flag + `MPDEV_SOURCE` env var | 跟随 mppm；env var 适合 CI/.bashrc 场景 |
| **D6 publish 机制** | `scripts/sync-to-gitlab.sh` 手动同步 | git subtree 复杂；CI 依赖外部基础设施；手动脚本最直接 |
| **D7 PowerShell sync 版本** | 不做 | sync 仅维护者用，Windows 维护者走 Git Bash |
| **D8 版本号** | v2.1.0 → v2.1.1 (PATCH) | 仅 installer + sync 脚本增强；命令/hooks/agents/templates 完全不变 |
| **D9 SSH 主机指纹** | install.sh 自动 ssh-keyscan（可 `--skip-ssh-keyscan`）| mppm 同模式；用户首次装内网时痛点最大 |
| **D10 sync 版本冲突** | GitLab 已有同 tag → AskUserQuestion 选覆盖/取消 | 防止误覆盖发布；用户主动 confirm 才推 |

## 4. 文件清单（11 个改动）

| 路径 | 改动 | 行数 |
|------|------|------|
| `mpdev/bin/install.sh` | 双源逻辑 + SSH 鉴权 + 6 步结构 | +60 |
| `mpdev/bin/install.ps1` | 双源逻辑（GitHub HTTPS / GitLab 走 Git Bash 内嵌） | +40 |
| `mpdev/scripts/sync-to-gitlab.sh` | 维护者同步脚本（新建） | ~60 |
| `mpdev/VERSION` | 2.1.0 → 2.1.1 | 1 |
| `mpdev/.claude-plugin/plugin.json` | version bump | 1 |
| `mpdev/.claude-plugin/marketplace.json` | version bump | 1 |
| `mpdev/CHANGELOG.md` | 加 [2.1.1] | +15 |
| `mpdev/docs/quickstart.md` | 默认改 GitLab 命令 + GitHub fallback | +10 |
| `mpdev/docs/upgrade-guide.md` | 双源安装命令对照 | +20 |
| `mpdev/README.md` | 安装命令双源 | +15 |
| `README.md` (顶层) | badge v2.1.1 + 安装命令双源 + 维护者 sync 说明 | +30 |

总计约 **255 行**。

## 5. install.sh 双源实现

### 5.1 顶部 SOURCE 配置

```bash
# ---- Source 配置 ----
SOURCE_GITLAB_URL="git@10.173.28.211:robot-ai/mppm/mpdev.git"
SOURCE_GITLAB_BRANCH="master"
SOURCE_GITLAB_SUBDIR=""

SOURCE_GITHUB_URL="https://github.com/wzhiwei0821-coward/superdev.git"
SOURCE_GITHUB_BRANCH="main"
SOURCE_GITHUB_SUBDIR="mpdev"

SOURCE_TYPE="${MPDEV_SOURCE:-gitlab}"
SKIP_SSH_KEYSCAN=false
TARGET="${MPDEV_TARGET:-$HOME/dev/mpdev}"
```

### 5.2 参数解析（替换现有 for arg in "$@" 块）

```bash
for arg in "$@"; do
  case $arg in
    --source=gitlab)    SOURCE_TYPE="gitlab"; shift ;;
    --source=github)    SOURCE_TYPE="github"; shift ;;
    --target=*)         TARGET="${arg#*=}"; shift ;;
    --repo=*)           REPO_URL_OVERRIDE="${arg#*=}"; shift ;;
    --branch=*)         BRANCH_OVERRIDE="${arg#*=}"; shift ;;
    --subdir=*)         SUBDIR_OVERRIDE="${arg#*=}"; shift ;;
    --skip-ssh-keyscan) SKIP_SSH_KEYSCAN=true; shift ;;
    -h|--help)
      cat <<EOF
Usage: install.sh [OPTIONS]

OPTIONS:
  --source=gitlab     (默认) 从公司 GitLab 装 (SSH)
  --source=github     从 GitHub 公网装 (HTTPS, 无需 SSH)
  --target=PATH       自定义本地 clone 路径（默认 \$HOME/dev/mpdev）
  --repo=URL          覆盖 source 的 clone URL（专家模式）
  --branch=NAME       覆盖 source 的分支
  --subdir=PATH       覆盖 source 的子目录（空字符串表示仓根 = plugin）
  --skip-ssh-keyscan  跳过 SSH 主机指纹自动接受
  -h, --help          显示此帮助

ENVIRONMENT:
  MPDEV_SOURCE=gitlab|github   等价 --source
  MPDEV_TARGET=PATH            等价 --target

EXAMPLES:
  bash install.sh                            # 默认 GitLab (SSH)
  bash install.sh --source=github            # 切公网 GitHub
  MPDEV_SOURCE=github bash install.sh        # env 等价
EOF
      exit 0 ;;
    *) echo "❌ 未知参数: $arg" ; exit 1 ;;
  esac
done

# 根据 SOURCE_TYPE 选 URL/branch/subdir（flag override 优先）
case $SOURCE_TYPE in
  gitlab)
    REPO_URL="${REPO_URL_OVERRIDE:-$SOURCE_GITLAB_URL}"
    BRANCH="${BRANCH_OVERRIDE:-$SOURCE_GITLAB_BRANCH}"
    SUBDIR="${SUBDIR_OVERRIDE-$SOURCE_GITLAB_SUBDIR}"
    ;;
  github)
    REPO_URL="${REPO_URL_OVERRIDE:-$SOURCE_GITHUB_URL}"
    BRANCH="${BRANCH_OVERRIDE:-$SOURCE_GITHUB_BRANCH}"
    SUBDIR="${SUBDIR_OVERRIDE-$SOURCE_GITHUB_SUBDIR}"
    ;;
  *) echo "❌ 未知 source: $SOURCE_TYPE (合法: gitlab / github)"; exit 1 ;;
esac
```

### 5.3 6 步结构改造

```
v2.1.0:                          v2.1.1:
  Step 1/4: 前置检查              Step 1/6: 前置检查
  Step 2/4: 克隆 mpdev            Step 2/6: SSH 主机指纹（仅 gitlab）
  Step 3/4: 版本信息              Step 3/6: 克隆 mpdev
  Step 4/4: 引导                  Step 4/6: 版本信息
  Step 5/5: BOM 自检              Step 5/6: 引导
                                  Step 6/6: hooks chmod + BOM 自检
```

### 5.4 SSH 鉴权块（Step 2/6，仅 gitlab）

```bash
if [ "$SOURCE_TYPE" = "gitlab" ] && [ "$SKIP_SSH_KEYSCAN" = false ]; then
  echo ""
  echo "▶ Step 2/6: SSH 主机指纹（gitlab 源需要）"
  SSH_HOST=$(echo "$REPO_URL" | sed -E 's|^git@([^:]+):.*|\1|')
  
  if ssh-keygen -F "$SSH_HOST" >/dev/null 2>&1; then
    echo "  ✅ $SSH_HOST 主机指纹已在 known_hosts"
  else
    echo "  ⚠️  $SSH_HOST 主机指纹未在 known_hosts"
    echo "      推荐：先手动 ssh -T git@$SSH_HOST 查看并接受指纹"
    read -p "      或自动 ssh-keyscan 添加？(y/N) " yn
    case $yn in
      [Yy]*) ssh-keyscan -H "$SSH_HOST" >> ~/.ssh/known_hosts 2>/dev/null && echo "  ✅ 已加入" ;;
      *) echo "  → 请手动 ssh -T git@$SSH_HOST 后重跑本脚本"; exit 4 ;;
    esac
  fi
  
  if ! ssh -T -o BatchMode=yes -o ConnectTimeout=5 "git@$SSH_HOST" 2>&1 | grep -qE "Welcome|successfully|GitLab"; then
    echo "  ❌ SSH 鉴权失败。可能原因："
    echo "      ① GitLab User Settings → SSH Keys 添加你的公钥（~/.ssh/id_*.pub）"
    echo "      ② SSH key 不存在 → ssh-keygen -t ed25519 -C \"your_email\""
    echo "      ③ 网络不通（VPN / 防火墙）→ ping $SSH_HOST"
    exit 4
  fi
  echo "  ✅ SSH 鉴权通过"
elif [ "$SOURCE_TYPE" = "github" ]; then
  echo ""
  echo "▶ Step 2/6: 跳过（GitHub 走 HTTPS，无需 SSH）"
fi
```

### 5.5 Clone 块（Step 3/6，支持 branch + 可空 SUBDIR）

```bash
echo ""
echo "▶ Step 3/6: 克隆 mpdev (source=$SOURCE_TYPE, branch=$BRANCH)"

if [ -d "$TARGET/.git" ]; then
  echo "  目标已是 git 仓库，pull 拉新"
  cd "$TARGET" && git pull origin "$BRANCH" 2>&1 | tail -3 && cd - >/dev/null
else
  TMP=$(mktemp -d)
  trap "rm -rf $TMP" EXIT
  
  if ! git clone --depth=1 --branch "$BRANCH" "$REPO_URL" "$TMP/repo" 2>&1 | tail -3; then
    echo "❌ git clone 失败"
    exit 3
  fi
  
  # 处理 SUBDIR（gitlab 是空，仓根；github 是 mpdev/）
  if [ -n "$SUBDIR" ]; then
    SUITE_ROOT="$TMP/repo/$SUBDIR"
    [ -d "$SUITE_ROOT" ] || { echo "❌ 找不到 $SUBDIR 子目录"; exit 3; }
  else
    SUITE_ROOT="$TMP/repo"
  fi
  
  mkdir -p "$(dirname "$TARGET")"
  cp -r "$SUITE_ROOT" "$TARGET"
  # 仅在 SUBDIR 非空时，目标会丢 .git（cp 子目录不含 .git）— 这是预期，install 不需要 .git
fi

# 验证 manifest
[ -f "$TARGET/.claude-plugin/marketplace.json" ] || { echo "❌ marketplace.json 缺失"; exit 3; }
echo "  ✅ $TARGET 就绪"
```

### 5.6 Step 4-6 不变

`Step 4/6 版本信息` / `Step 5/6 引导` / `Step 6/6 hooks chmod + BOM` — 原有逻辑保留，仅 Step 编号从 X/4 / X/5 → X/6 改名。

## 6. install.ps1 双源实现

PowerShell 等价逻辑。不做 SSH 鉴权（PS 上 SSH key 处理较麻烦，提示用户先手动 `ssh -T git@10.173.28.211` 后再跑），但保留 `--source` flag。

关键差异：PowerShell 用 `&` 调用 git clone 而非 bash 子进程。SUBDIR 为空时直接 `Copy-Item -Path "$Tmp/repo/*" -Destination $Target -Recurse`。

详见 plan 文档（writing-plans 出 task 时展开完整 PS 代码）。

## 7. scripts/sync-to-gitlab.sh 完整实现

```bash
#!/usr/bin/env bash
# sync-to-gitlab.sh — 把 superdev/mpdev/ 同步到 GitLab 独立 mpdev 仓
#
# Usage: 在 superdev/ 根跑
#   bash mpdev/scripts/sync-to-gitlab.sh
#   bash mpdev/scripts/sync-to-gitlab.sh --dry-run
#
# Environment:
#   MPDEV_GITLAB_REPO    覆盖 GitLab clone URL
#   MPDEV_GITLAB_BRANCH  覆盖 GitLab 分支（默认 master）
#   MPDEV_SOURCE_DIR     覆盖本地 mpdev 子目录（默认 mpdev）

set -e

GITLAB_REPO="${MPDEV_GITLAB_REPO:-git@10.173.28.211:robot-ai/mppm/mpdev.git}"
GITLAB_BRANCH="${MPDEV_GITLAB_BRANCH:-master}"
SOURCE_DIR="${MPDEV_SOURCE_DIR:-mpdev}"
DRY_RUN=false

for arg in "$@"; do
  case $arg in
    --dry-run) DRY_RUN=true; shift ;;
    -h|--help)
      sed -n '2,12p' "$0" | sed 's|^# \?||'
      exit 0 ;;
    *) echo "❌ 未知参数: $arg"; exit 1 ;;
  esac
done

# Preflight
[ -d "$SOURCE_DIR" ] || { echo "❌ $SOURCE_DIR/ 不存在 (是否在 superdev/ 根?)"; exit 1; }
[ -f "$SOURCE_DIR/VERSION" ] || { echo "❌ $SOURCE_DIR/VERSION 缺失"; exit 1; }
command -v rsync >/dev/null || { echo "❌ rsync 未安装（Mac/Linux 自带；Git Bash 需 pacman -S rsync）"; exit 1; }

VERSION=$(cat "$SOURCE_DIR/VERSION")
SUPERDEV_SHA=$(git rev-parse --short HEAD 2>/dev/null || echo "unknown")

echo "▶ 同步 mpdev v$VERSION (superdev@$SUPERDEV_SHA)"
echo "  目标: $GITLAB_REPO ($GITLAB_BRANCH)"
$DRY_RUN && echo "  [DRY-RUN 模式，不会真 push]"
echo ""

# Clone GitLab 仓
TMP=$(mktemp -d); trap "rm -rf $TMP" EXIT
echo "▶ Step 1/4: Clone GitLab 仓到临时目录"
git clone --depth=1 --branch "$GITLAB_BRANCH" "$GITLAB_REPO" "$TMP/gitlab" 2>&1 | tail -2

# 检查版本冲突
if (cd "$TMP/gitlab" && git tag -l "v$VERSION" | grep -q .); then
  echo "⚠️  GitLab 仓已存在 v$VERSION tag"
  read -p "覆盖（不推荐 — 删 tag + force push）/ 取消？(y/N) " yn
  case $yn in
    [Yy]*) (cd "$TMP/gitlab" && git tag -d "v$VERSION" && git push origin ":refs/tags/v$VERSION" 2>/dev/null || true) ;;
    *) echo "取消"; exit 2 ;;
  esac
fi

# rsync 同步
echo ""
echo "▶ Step 2/4: rsync $SOURCE_DIR/ → GitLab 临时副本"
rsync -av --delete --exclude='.git' "$SOURCE_DIR/" "$TMP/gitlab/"

# 提交 + tag
echo ""
echo "▶ Step 3/4: commit + tag"
cd "$TMP/gitlab"
git add -A
if git diff --cached --quiet; then
  echo "  ℹ️ GitLab 已是最新，无需提交"
  exit 0
fi
git -c user.email='mpdev-sync@local' -c user.name='mpdev-sync' commit -m "release: v$VERSION

Synced from superdev@$SUPERDEV_SHA via sync-to-gitlab.sh"
git tag "v$VERSION"

# Push
echo ""
if $DRY_RUN; then
  echo "▶ Step 4/4: [DRY-RUN] 跳过 push。下次去掉 --dry-run 即可推送。"
  echo "  将推送的内容:"
  git log -1 --stat
else
  echo "▶ Step 4/4: Push 到 GitLab"
  git push origin "$GITLAB_BRANCH"
  git push origin "v$VERSION"
fi

echo ""
echo "✅ 同步完成: v$VERSION"
echo "   GitLab: http://10.173.28.211/robot-ai/mppm/mpdev/-/tree/$GITLAB_BRANCH"
```

## 8. CHANGELOG [2.1.1] 草稿

```markdown
## [2.1.1] — 2026-05-18

### Added
- **install.sh / install.ps1 双源支持**：
  - 默认 GitLab 内网 (`git@10.173.28.211:robot-ai/mppm/mpdev.git` SSH)
  - `--source=github` 或 `MPDEV_SOURCE=github` 切公网 GitHub HTTPS
  - install.sh 新增 SSH 主机指纹自动接受（可 `--skip-ssh-keyscan` 关）
  - install.sh / install.ps1 6 步结构（Step 2 新增 SSH 鉴权）
- **`scripts/sync-to-gitlab.sh` 维护者同步脚本**：
  - 在 `superdev/` 根跑，rsync `mpdev/` → GitLab 独立 mpdev 仓
  - 支持 `--dry-run`
  - 检测 v 版本 tag 冲突时弹询问
  - 通过 `MPDEV_GITLAB_REPO` / `MPDEV_GITLAB_BRANCH` / `MPDEV_SOURCE_DIR` env 覆盖

### Notes
- v2.1.0 用户的 `~/dev/mpdev/.git` 仍指向原 origin（不重装则 install.sh 不动它）
- `/plugin update` 路径不变；通过 `~/dev/mpdev` 本地仓的 origin 决定升级源
- PowerShell sync 版本不做（维护者走 Git Bash）

### Notes (Internal/Maintainer)
- GitLab 仓: http://10.173.28.211/robot-ai/mppm/mpdev
- 发布步骤: superdev 改完 → push GitHub origin → `bash mpdev/scripts/sync-to-gitlab.sh`
```

## 9. 验收剧本（7 项）

| # | 场景 | 期望 |
|---|------|------|
| 1 | `bash install.sh --help` | 输出含 `--source=gitlab`/`--source=github`/`MPDEV_SOURCE` |
| 2 | `bash install.sh --source=github` 沙盒模拟 | clone GitHub → `~/dev/mpdev` 含 plugin 全部内容 |
| 3 | `MPDEV_SOURCE=github bash install.sh` | env var 等价 flag |
| 4 | `bash install.sh --source=gitlab --skip-ssh-keyscan` （无网络） | clone 失败，提示 SSH 鉴权问题 |
| 5 | `bash install.sh --bogus` | 报错 exit 1 |
| 6 | `bash sync-to-gitlab.sh --dry-run`（在 superdev 根） | rsync 跑完不 push，输出 dry-run 标识 |
| 7 | sync-to-gitlab.sh 在非 superdev 目录跑 | exit 1 "❌ mpdev/ 不存在" |

## 10. 风险与对策

| 风险 | 概率 | 影响 | 对策 |
|------|------|------|------|
| 默认改 GitLab 但 GitLab 仓不存在/未授权 | 高 | 高 | spec §15 明确：实施前**用户先在 GitLab 建好 robot-ai/mppm/mpdev 仓 + 加 SSH key**；plan §T-pre 加 preflight check task |
| 内外网混用：内网用户用 GitHub URL（公司可能不通） | 中 | 中 | 文档建议公网命令仅供外部用户；内网用户用默认即可 |
| sync 脚本 rsync `--delete` 误删 GitLab 仓里的本地分支 / 文件 | 低 | 高 | rsync 在临时 clone 跑，不影响 GitLab 本仓；--exclude='.git' 保留版本控制；commit 前用 git diff --cached 给用户最后看一眼（在 --dry-run 体现） |
| sync 版本冲突时用户误选「覆盖」force push 破坏历史 | 低 | 高 | 询问语用「不推荐」明示风险；commit 用同步 sync 用户名（mpdev-sync）便于审计 |
| GitHub HTTPS 在公司网络被代理拦截 | 低 | 低 | 提示用户配置 git proxy；或 fallback SSH `git@github.com:wzhiwei0821-coward/superdev.git` |
| Windows 用户跑 sync.sh 失败（无 rsync） | 中 | 低 | preflight 显式检查 rsync；提示 Git Bash 用 pacman 装 |

## 11. 不在 v2.1.1 范围

- ❌ PowerShell sync 脚本 (`sync-to-gitlab.ps1`) — 维护者走 Git Bash
- ❌ 自动检测网络（ping 10.173.28.211 ↔ fallback github）— 不透明，调试困难
- ❌ GitHub Actions / GitLab CI 自动镜像 — 依赖外部基础设施
- ❌ `--source=both` 同时拉两个源 — YAGNI
- ❌ Token 鉴权（HTTPS+PAT）替代 SSH — mppm 同模式仅 SSH，保持一致
- ❌ 改命令 / hooks / agents / templates 行为

## 12. 实施阶段

| 阶段 | 工作 | 估时 |
|------|------|------|
| 1 | install.sh 双源 + SSH 鉴权 + 6 步结构 + --help | 45 min |
| 2 | install.ps1 双源（无 SSH 测试） | 30 min |
| 3 | scripts/sync-to-gitlab.sh 完整实现 + --dry-run | 30 min |
| 4 | VERSION / plugin.json / marketplace.json bump 2.1.1 | 10 min |
| 5 | CHANGELOG + README + quickstart + upgrade-guide + 顶层 README badge | 40 min |
| 6 | 7 个验收剧本沙盒实测 | 40 min |

**合计约 3 小时**（单人）。

## 13. 发布步骤

```bash
# 1. superdev 改完
cd ~/superdev
git add -A && git commit -m "release: v2.1.1 — dual source"

# 2. push GitHub origin
git tag mpdev-v2.1.1
git push origin main --tags

# 3. 同步 GitLab（新增步骤）
bash mpdev/scripts/sync-to-gitlab.sh
```

执行完成后：
- GitHub 用户：跑 `bash install.sh --source=github` 拉到 v2.1.1
- GitLab 用户：跑 `bash install.sh` (默认 gitlab) 拉到 v2.1.1
- v2.1.0 用户：`/plugin update` 拉 v2.1.1（按本地仓 origin 而定）
