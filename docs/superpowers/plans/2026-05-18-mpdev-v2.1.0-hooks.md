# mpdev v2.1.0 Hooks Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v2.1.0 minor — 3 个 Claude Code Hooks（SessionStart 注入 CLAUDE.md 摘要 / UserPromptSubmit 关键词建议命令 / SubagentStop 检测 cross_module_issue）+ MPDEV_NO_HOOKS=1 一开关。

**Architecture:** `mpdev/hooks/` 新目录含 1 个 hooks.json + 1 个 `_lib.sh` 共享工具 + 3 个独立 .sh hook 脚本。plugin.json 加 `"hooks": "./hooks/hooks.json"` 字段。install 脚本末尾 chmod +x + LF 行尾保护。

**Tech Stack:** Bash 4+ (Windows 走 Git Bash) / jq (优先，无则退化 grep+sed) / Claude Code Hook spec / UTF-8 BOM。

**Spec:** [`docs/superpowers/specs/2026-05-18-mpdev-v2.1.0-hooks.md`](../specs/2026-05-18-mpdev-v2.1.0-hooks.md)

---

## File Structure

### 新建文件（5 个）

| 路径 | 责任 | 行数 |
|------|------|------|
| `mpdev/hooks/hooks.json` | 注册 3 个 hook event 到 Claude Code plugin runtime | ~50 |
| `mpdev/hooks/_lib.sh` | 共享 bash 工具（check_disabled / get_project_dir / extract_frontmatter / get_yaml_field） | ~40 |
| `mpdev/hooks/session-start.sh` | #1 SessionStart hook — 注入 CLAUDE.md 摘要 | ~65 |
| `mpdev/hooks/inject-keyword-hint.sh` | #2 UserPromptSubmit hook — 关键词命中建议命令 | ~65 |
| `mpdev/hooks/post-subagent-check.sh` | #5 SubagentStop hook — 检测 cross_module_issue | ~55 |

### 修改文件（9 个）

| 路径 | 改动 |
|------|------|
| `mpdev/.claude-plugin/plugin.json` | 加 `"hooks": "./hooks/hooks.json"`；version `2.0.1` → `2.1.0` |
| `mpdev/.claude-plugin/marketplace.json` | metadata version `2.0.1` → `2.1.0` |
| `mpdev/VERSION` | `2.0.1` → `2.1.0` |
| `mpdev/CHANGELOG.md` | 加 `[2.1.0]` 段 |
| `mpdev/docs/troubleshooting.md` | 加 "Hooks" 节 |
| `mpdev/README.md` | 数据架构图加 hooks 层 + 列 3 个 hook |
| `README.md` (顶层) | mpdev badge `v2.0.1` → `v2.1.0` |
| `mpdev/bin/install.sh` | 末尾加 chmod +x hooks/*.sh |
| `mpdev/bin/install.ps1` | 末尾加 .sh 文件 CRLF→LF 转换 |

---

## Task 1: 建 hooks/ 目录 + hooks.json

**Files:**
- Create: `mpdev/hooks/hooks.json`

- [ ] **Step 1: 建目录 + 写 hooks.json**

```bash
cd F:/claude/superdev && mkdir -p mpdev/hooks
```

写入 `F:/claude/superdev/mpdev/hooks/hooks.json` 以下完整内容：

```json
{
  "$schema": "https://json.schemastore.org/claude-code-plugin-hooks.json",
  "_comment": "mpdev plugin hooks 注册。所有 hook fail-silent，MPDEV_NO_HOOKS=1 一开关全禁。",
  "_dependencies": "bash 4+ / jq / grep / awk / sed（POSIX 标准工具）",
  "_data_decoupling": "所有 hook 只读，绝不写入项目数据（.claude/agents/ / .claude/mpdev-runs/ 等）",
  "hooks": {
    "SessionStart": [
      {
        "_purpose": "扫描项目里 CLAUDE.md，提取技术栈摘要注入 Claude 上下文",
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh",
            "timeout": 5,
            "async": false
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "_purpose": "用户输入含 mpdev 工作流关键词时，systemMessage 建议对应 /mpdev:* 命令",
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/inject-keyword-hint.sh",
            "timeout": 3,
            "async": false
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "_purpose": "impl agent 输出 cross_module_issue 时，additionalContext 建议下一步 /mpdev:fix",
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/post-subagent-check.sh",
            "timeout": 3,
            "async": false
          }
        ]
      }
    ]
  }
}
```

- [ ] **Step 2: 验证 JSON 合法**

```bash
cd F:/claude/superdev && python -c "import json,sys; sys.stdout.reconfigure(encoding='utf-8'); h=json.load(open('mpdev/hooks/hooks.json',encoding='utf-8')); print('events:', list(h['hooks'].keys()))"
```

Expected: `events: ['SessionStart', 'UserPromptSubmit', 'SubagentStop']`

- [ ] **Step 3: Commit**

```bash
cd F:/claude/superdev && git add mpdev/hooks/hooks.json && git commit -m "feat(v2.1.0): hooks/hooks.json — register 3 hook events

SessionStart / UserPromptSubmit / SubagentStop 注册到 Claude Code plugin runtime。
所有 hook 用 \${CLAUDE_PLUGIN_ROOT}/hooks/*.sh 调用，3-5s 超时，async=false。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: 共享工具 _lib.sh

**Files:**
- Create: `mpdev/hooks/_lib.sh`

- [ ] **Step 1: 写入 _lib.sh**

写入 `F:/claude/superdev/mpdev/hooks/_lib.sh` 以下完整内容：

```bash
#!/usr/bin/env bash
# hooks/_lib.sh — 共享工具，被 3 个 hook source
# 设计原则：fail-silent，绝不阻塞会话；只读不写

# 用户禁用 → 立即返回 {} 并 exit 0
check_disabled() {
  if [ -n "$MPDEV_NO_HOOKS" ]; then
    echo "{}"
    exit 0
  fi
}

# 找项目根（Claude Code 注入 CLAUDE_PROJECT_DIR；fallback 到 PWD）
get_project_dir() {
  echo "${CLAUDE_PROJECT_DIR:-$PWD}"
}

# 从文件提取 YAML frontmatter 块（首个 --- ~ --- 之间）
extract_frontmatter() {
  awk '/^---$/{c++; next} c==1{print} c==2{exit}' "$1"
}

# 从 frontmatter / markdown 抽取指定 key 的值
get_yaml_field() {
  local content="$1"
  local key="$2"
  echo "$content" | grep -E "^${key}:" | head -1 | sed -E "s/^${key}:[[:space:]]*//; s/^['\"]//; s/['\"]$//"
}

# 输出 hookSpecificOutput JSON（用 jq 优先；jq 不可用退化）
output_additional_context() {
  local event_name="$1"
  local ctx="$2"
  if command -v jq >/dev/null 2>&1; then
    jq -n --arg event "$event_name" --arg ctx "$ctx" '{
      hookSpecificOutput: {
        hookEventName: $event,
        additionalContext: $ctx
      }
    }'
  else
    local esc_ctx
    esc_ctx=$(echo "$ctx" | sed 's/\\/\\\\/g; s/"/\\"/g' | awk '{printf "%s\\n", $0}')
    echo "{\"hookSpecificOutput\":{\"hookEventName\":\"${event_name}\",\"additionalContext\":\"${esc_ctx}\"}}"
  fi
}

# 输出 hookSpecificOutput JSON with systemMessage（同上但 message 显式给用户）
output_system_message() {
  local event_name="$1"
  local msg="$2"
  if command -v jq >/dev/null 2>&1; then
    jq -n --arg event "$event_name" --arg msg "$msg" '{
      hookSpecificOutput: {
        hookEventName: $event,
        systemMessage: $msg
      }
    }'
  else
    local esc_msg
    esc_msg=$(echo "$msg" | sed 's/\\/\\\\/g; s/"/\\"/g' | awk '{printf "%s\\n", $0}')
    echo "{\"hookSpecificOutput\":{\"hookEventName\":\"${event_name}\",\"systemMessage\":\"${esc_msg}\"}}"
  fi
}
```

- [ ] **Step 2: 验证 bash 语法**

```bash
cd F:/claude/superdev && bash -n mpdev/hooks/_lib.sh && echo "syntax ok"
```

Expected: `syntax ok`

- [ ] **Step 3: 测试 check_disabled 函数**

```bash
cd F:/claude/superdev && bash -c "source mpdev/hooks/_lib.sh; check_disabled; echo 'should not reach here'" && echo "---" && \
  MPDEV_NO_HOOKS=1 bash -c "source mpdev/hooks/_lib.sh; check_disabled; echo 'should not reach here'"
```

Expected: 第一次输出 "should not reach here"（变量未设，不退出）；第二次输出 `{}` 后无后续（因为 exit 0）

- [ ] **Step 4: Commit**

```bash
cd F:/claude/superdev && git add mpdev/hooks/_lib.sh && git commit -m "feat(v2.1.0): hooks/_lib.sh — shared utility functions

5 个共享 fn: check_disabled / get_project_dir / extract_frontmatter /
get_yaml_field / output_additional_context / output_system_message。
3 个 hook 通过 source 复用，避免 30 行重复代码。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: session-start.sh hook (#1)

**Files:**
- Create: `mpdev/hooks/session-start.sh`

- [ ] **Step 1: 写入 session-start.sh**

写入 `F:/claude/superdev/mpdev/hooks/session-start.sh` 以下完整内容：

```bash
#!/usr/bin/env bash
# session-start.sh — mpdev SessionStart hook
#
# 触发: Claude Code 启动 / 新会话
# 输入: stdin {} (SessionStart 事件无 payload)
# 输出: hookSpecificOutput JSON with additionalContext
# 失败策略: 任何错误都退回 {}，绝不阻塞会话

set -e

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
# shellcheck disable=SC1091
source "$SCRIPT_DIR/_lib.sh"

check_disabled

PROJECT_DIR=$(get_project_dir)

# Glob CLAUDE.md 排除明显非业务目录
mapfile -t CLAUDE_FILES < <(find "$PROJECT_DIR" \
  -name CLAUDE.md \
  -not -path '*/node_modules/*' \
  -not -path '*/.git/*' \
  -not -path '*/.claude/plugins/*' \
  -not -path '*/target/*' \
  -not -path '*/dist/*' \
  -not -path '*/build/*' \
  2>/dev/null)

# 0 个 → 静默退出
[ ${#CLAUDE_FILES[@]} -eq 0 ] && { echo "{}"; exit 0; }

# 超过 5 个 → 取前 5
COUNT_TOTAL=${#CLAUDE_FILES[@]}
if [ $COUNT_TOTAL -gt 5 ]; then
  CLAUDE_FILES=("${CLAUDE_FILES[@]:0:5}")
fi
COUNT_INCLUDED=${#CLAUDE_FILES[@]}

# 拼接摘要
summary=""
for f in "${CLAUDE_FILES[@]}"; do
  rel="${f#$PROJECT_DIR/}"
  module=$(basename "$(dirname "$f")")
  # 提取 "## 技术栈" 节（限 10 行）
  tech=$(awk '/^## 技术栈/{f=1;next} /^## /{f=0} f' "$f" | head -10)
  if [ -n "$tech" ]; then
    summary+="### ${module} (${rel}):
${tech}

"
  else
    summary+="### ${module} (${rel}): (无 '技术栈' 节)

"
  fi
done

# 构造 additionalContext
omitted=""
if [ $COUNT_TOTAL -gt $COUNT_INCLUDED ]; then
  omitted="（还有 $((COUNT_TOTAL - COUNT_INCLUDED)) 个 CLAUDE.md 未注入，按需用 /mpdev:understand 重新理解）"
fi

context="## mpdev: 检测到 ${COUNT_INCLUDED} 个模块 CLAUDE.md ${omitted}

${summary}
（可调 /mpdev:understand 重新生成或更新；/mpdev:dev 启动跨模块开发主流程。）"

output_additional_context "SessionStart" "$context"
```

- [ ] **Step 2: 验证 bash 语法**

```bash
cd F:/claude/superdev && bash -n mpdev/hooks/session-start.sh && echo "syntax ok"
```

Expected: `syntax ok`

- [ ] **Step 3: 测试 — 无 CLAUDE.md 项目**

```bash
cd F:/claude/superdev && \
  rm -rf /tmp/hook-t3-empty && mkdir -p /tmp/hook-t3-empty && \
  cd /tmp/hook-t3-empty && \
  echo '{}' | bash F:/claude/superdev/mpdev/hooks/session-start.sh
```

Expected: `{}`（项目无 CLAUDE.md → 静默退出）

- [ ] **Step 4: 测试 — 有 CLAUDE.md 项目**

```bash
cd F:/claude/superdev && \
  rm -rf /tmp/hook-t3-claude && mkdir -p /tmp/hook-t3-claude/java && \
  cat > /tmp/hook-t3-claude/java/CLAUDE.md << 'EOF'
# Java 后端

## 技术栈

- Java 1.8
- Spring Boot 2.3.12
- MyBatis-Plus 3.4.0

## 目录结构

src/main/java/...
EOF
  cd /tmp/hook-t3-claude && \
  echo '{}' | bash F:/claude/superdev/mpdev/hooks/session-start.sh | head -10
```

Expected: 含 `"hookSpecificOutput"` + `"hookEventName": "SessionStart"` + `"additionalContext"` 含 "java" 和 "Spring Boot 2.3.12"

- [ ] **Step 5: 测试 — MPDEV_NO_HOOKS=1**

```bash
cd /tmp/hook-t3-claude && MPDEV_NO_HOOKS=1 bash F:/claude/superdev/mpdev/hooks/session-start.sh
```

Expected: `{}`（即使有 CLAUDE.md 也静默）

- [ ] **Step 6: 清理 + Commit**

```bash
cd F:/claude/superdev && \
  rm -rf /tmp/hook-t3-empty /tmp/hook-t3-claude && \
  git add mpdev/hooks/session-start.sh && \
  git commit -m "feat(v2.1.0): hooks/session-start.sh — inject CLAUDE.md summary

Glob 项目里 CLAUDE.md（排除 node_modules/.git/target/dist/build），提取
'## 技术栈' 节摘要注入 additionalContext。超过 5 个 CLAUDE.md 取头 5 个 +
提示剩余数量。0 个静默。MPDEV_NO_HOOKS=1 全禁。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: inject-keyword-hint.sh hook (#2)

**Files:**
- Create: `mpdev/hooks/inject-keyword-hint.sh`

- [ ] **Step 1: 写入 inject-keyword-hint.sh**

写入 `F:/claude/superdev/mpdev/hooks/inject-keyword-hint.sh` 以下完整内容：

```bash
#!/usr/bin/env bash
# inject-keyword-hint.sh — mpdev UserPromptSubmit hook
#
# 触发: 用户提交 prompt
# 输入: stdin JSON with .prompt
# 输出: hookSpecificOutput with systemMessage (命中时) 或 {} (未命中)

set -e

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
# shellcheck disable=SC1091
source "$SCRIPT_DIR/_lib.sh"

check_disabled

# 读 stdin → .prompt
if command -v jq >/dev/null 2>&1; then
  PROMPT=$(cat | jq -r '.prompt // ""')
else
  PROMPT=$(cat | grep -oP '"prompt":\s*"\K[^"]*' | head -1)
fi

# 已在用 /mpdev:* 命令 → 跳过自我推荐
if echo "$PROMPT" | grep -qE '^/mpdev:'; then
  echo "{}"
  exit 0
fi

# 关键词模式 → 命令映射（按顺序匹配，先命中先返回）
# 用并行数组避免 declare -A 在某些 bash 版本兼容性问题
PATTERNS=(
  "修.*bug|fix.*bug|NPE|空指针|异常|报错|stacktrace"
  "新.*需求|新功能|增加.*字段|加.*接口|加.*表|新.*API"
  "理解.*项目|新项目|没有 CLAUDE|代码不熟|看不懂"
  "启动.*服务|起.*环境|环境.*状态|重启.*服务"
  "写.*测试|加.*测试|测试覆盖|test case"
  "检查.*契约|跨模块.*一致|联调|契约.*漂移"
  "生成 commit|提交.*说明|改了什么|commit message"
)
COMMANDS=(
  "fix"
  "dev"
  "understand"
  "env"
  "test"
  "check"
  "commit"
)

for i in "${!PATTERNS[@]}"; do
  if echo "$PROMPT" | grep -qE "${PATTERNS[$i]}"; then
    cmd="${COMMANDS[$i]}"
    msg="💡 检测到与 \`/mpdev:${cmd}\` 相关的关键词。如果想用 mpdev 工作流处理，可以试试：

\`\`\`
/mpdev:${cmd} <补充信息>
\`\`\`

（继续当前对话不受影响）"
    output_system_message "UserPromptSubmit" "$msg"
    exit 0
  fi
done

echo "{}"
```

- [ ] **Step 2: 验证 bash 语法**

```bash
cd F:/claude/superdev && bash -n mpdev/hooks/inject-keyword-hint.sh && echo "syntax ok"
```

Expected: `syntax ok`

- [ ] **Step 3: 测试 — 命中 "修 bug" 关键词**

```bash
cd F:/claude/superdev && echo '{"prompt":"帮我修个 NPE bug"}' | bash mpdev/hooks/inject-keyword-hint.sh
```

Expected: 输出 JSON 含 `"systemMessage"` 字段，含 `/mpdev:fix`

- [ ] **Step 4: 测试 — 用户已用 /mpdev: 命令**

```bash
cd F:/claude/superdev && echo '{"prompt":"/mpdev:fix vue 下拉框 bug"}' | bash mpdev/hooks/inject-keyword-hint.sh
```

Expected: `{}`（防自我推荐）

- [ ] **Step 5: 测试 — 无关 prompt**

```bash
cd F:/claude/superdev && echo '{"prompt":"今天天气怎么样"}' | bash mpdev/hooks/inject-keyword-hint.sh
```

Expected: `{}`

- [ ] **Step 6: 测试 — 命中 "新需求" 关键词 → /mpdev:dev**

```bash
cd F:/claude/superdev && echo '{"prompt":"我有个新需求，要加 night_patrol 任务类型"}' | bash mpdev/hooks/inject-keyword-hint.sh
```

Expected: 输出 JSON 含 `/mpdev:dev`

- [ ] **Step 7: 测试 — MPDEV_NO_HOOKS=1**

```bash
cd F:/claude/superdev && MPDEV_NO_HOOKS=1 echo '{"prompt":"修 bug"}' | bash mpdev/hooks/inject-keyword-hint.sh
```

注意：`MPDEV_NO_HOOKS=1 echo ...` 不会把变量传给 bash 子进程。正确写法：

```bash
cd F:/claude/superdev && echo '{"prompt":"修 bug"}' | MPDEV_NO_HOOKS=1 bash mpdev/hooks/inject-keyword-hint.sh
```

Expected: `{}`

- [ ] **Step 8: Commit**

```bash
cd F:/claude/superdev && git add mpdev/hooks/inject-keyword-hint.sh && git commit -m "feat(v2.1.0): hooks/inject-keyword-hint.sh — keyword-based command suggestion

7 个关键词模式 → 7 个 /mpdev:* 命令映射。命中时 systemMessage 提示用户。
prompt 以 /mpdev: 开头时静默（防自我推荐）。无关键词时 {}。
MPDEV_NO_HOOKS=1 全禁。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: post-subagent-check.sh hook (#5)

**Files:**
- Create: `mpdev/hooks/post-subagent-check.sh`

- [ ] **Step 1: 写入 post-subagent-check.sh**

写入 `F:/claude/superdev/mpdev/hooks/post-subagent-check.sh` 以下完整内容：

```bash
#!/usr/bin/env bash
# post-subagent-check.sh — mpdev SubagentStop hook
#
# 触发: subagent 跑完
# 输入: stdin JSON; 兼容 3 种可能字段名（.subagent_response/.output/.response）
# 输出: hookSpecificOutput with additionalContext (cross_module_issue 命中) 或 {}

set -e

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
# shellcheck disable=SC1091
source "$SCRIPT_DIR/_lib.sh"

check_disabled

# 读 stdin → subagent 输出文本（兼容多种字段名）
if command -v jq >/dev/null 2>&1; then
  RESPONSE=$(cat | jq -r '.subagent_response // .output // .response // ""')
else
  RESPONSE=$(cat)
fi

# 提取 cross_module_issue 字段
ISSUE=$(echo "$RESPONSE" | grep -E 'cross_module_issue:[[:space:]]*[^[:space:]]' | head -1)
[ -z "$ISSUE" ] && { echo "{}"; exit 0; }

# null / "" / 空值 → 跳过
if echo "$ISSUE" | grep -qE 'cross_module_issue:[[:space:]]*(null|""|''|\s*$)'; then
  echo "{}"
  exit 0
fi

# 解析 value
DESC=$(echo "$ISSUE" | sed -E 's/cross_module_issue:[[:space:]]*//; s/^"//; s/"$//; s/^[[:space:]]*//')

# 启发式抽模块名（匹配常见 mpdev 模块命名）
TARGET=$(echo "$DESC" | grep -oE '\b(java|vue|dispatch|analytics|algor|h5|pad|web|frontend|backend)\b' | head -1)

# 构造 additionalContext
if [ -n "$TARGET" ]; then
  suggestion="

建议下一步：\`/mpdev:fix ${TARGET} <bug 描述>\`"
else
  suggestion="

建议检查 cross_module_issue 涉及的模块，跑 \`/mpdev:fix <module> <bug>\`。"
fi

context="## 跨模块影响提示

上一步 impl agent 反馈跨模块影响：
> ${DESC}
${suggestion}"

output_additional_context "SubagentStop" "$context"
```

- [ ] **Step 2: 验证 bash 语法**

```bash
cd F:/claude/superdev && bash -n mpdev/hooks/post-subagent-check.sh && echo "syntax ok"
```

Expected: `syntax ok`

- [ ] **Step 3: 测试 — subagent 输出含 cross_module_issue**

```bash
cd F:/claude/superdev && cat << 'EOF' | bash mpdev/hooks/post-subagent-check.sh
{"subagent_response": "status: ok\ncross_module_issue: 修 java NPE 时发现 dispatch 模块也需要补 null 检查"}
EOF
```

Expected: 输出 JSON 含 `"additionalContext"` 含 "跨模块影响" 和 "/mpdev:fix dispatch"

- [ ] **Step 4: 测试 — cross_module_issue 为 null**

```bash
cd F:/claude/superdev && cat << 'EOF' | bash mpdev/hooks/post-subagent-check.sh
{"subagent_response": "status: ok\ncross_module_issue: null"}
EOF
```

Expected: `{}`

- [ ] **Step 5: 测试 — subagent 输出无 cross_module_issue 字段**

```bash
cd F:/claude/superdev && cat << 'EOF' | bash mpdev/hooks/post-subagent-check.sh
{"subagent_response": "status: ok\nfiles_changed: [src/...]\nroot_cause: NPE"}
EOF
```

Expected: `{}`

- [ ] **Step 6: 测试 — MPDEV_NO_HOOKS=1**

```bash
cd F:/claude/superdev && echo '{"subagent_response": "cross_module_issue: java 模块也要改"}' | MPDEV_NO_HOOKS=1 bash mpdev/hooks/post-subagent-check.sh
```

Expected: `{}`

- [ ] **Step 7: Commit**

```bash
cd F:/claude/superdev && git add mpdev/hooks/post-subagent-check.sh && git commit -m "feat(v2.1.0): hooks/post-subagent-check.sh — detect cross_module_issue

读 subagent 输出（兼容 .subagent_response/.output/.response 三字段名）。
检测 cross_module_issue: <非null/空> 字段，启发式抽取模块名（java/vue/
dispatch/analytics/algor/h5/pad/web/frontend/backend）。additionalContext
建议 /mpdev:fix <module>。MPDEV_NO_HOOKS=1 全禁。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: plugin.json + VERSION + marketplace.json bump 2.1.0

**Files:**
- Modify: `mpdev/.claude-plugin/plugin.json`
- Modify: `mpdev/VERSION`
- Modify: `mpdev/.claude-plugin/marketplace.json`

- [ ] **Step 1: 改 plugin.json — 加 hooks 字段 + version bump**

Read `F:/claude/superdev/mpdev/.claude-plugin/plugin.json` 确认当前结构。

Use Edit tool:

old_string:
```
  "version": "2.0.1",
```

new_string:
```
  "version": "2.1.0",
```

然后第二个 Edit 加 hooks 字段。Read 文件确认 keywords 数组结尾在哪里，找一个唯一的 anchor。

old_string (找文件末尾的 `]\n}`):
```
    "workflow"
  ]
}
```

new_string:
```
    "workflow"
  ],
  "hooks": "./hooks/hooks.json"
}
```

- [ ] **Step 2: 改 VERSION**

Use Edit tool on `mpdev/VERSION`:
- old_string: `2.0.1`
- new_string: `2.1.0`

- [ ] **Step 3: 改 marketplace.json**

Use Edit tool on `mpdev/.claude-plugin/marketplace.json`:
- old_string: `"version": "2.0.1"`
- new_string: `"version": "2.1.0"`

- [ ] **Step 4: 验证**

```bash
cd F:/claude/superdev && \
  cat mpdev/VERSION && \
  echo "" && \
  python -c "import json,sys; sys.stdout.reconfigure(encoding='utf-8'); p=json.load(open('mpdev/.claude-plugin/plugin.json',encoding='utf-8')); m=json.load(open('mpdev/.claude-plugin/marketplace.json',encoding='utf-8')); print('plugin.json version:', p['version']); print('plugin.json hooks:', p.get('hooks', 'MISSING')); print('marketplace metadata version:', m['metadata']['version'])"
```

Expected:
```
2.1.0

plugin.json version: 2.1.0
plugin.json hooks: ./hooks/hooks.json
marketplace metadata version: 2.1.0
```

- [ ] **Step 5: Commit**

```bash
cd F:/claude/superdev && git add mpdev/VERSION mpdev/.claude-plugin/plugin.json mpdev/.claude-plugin/marketplace.json && git commit -m "release: bump to v2.1.0 + register hooks in plugin.json

plugin.json 加 \"hooks\": \"./hooks/hooks.json\" 字段。
VERSION + plugin.json + marketplace.json 三处 2.0.1 → 2.1.0。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 7: install.sh 加 chmod +x hooks/*.sh

**Files:**
- Modify: `mpdev/bin/install.sh`

- [ ] **Step 1: 在 BOM 自检之前加 chmod 步骤**

Read `F:/claude/superdev/mpdev/bin/install.sh` 确认 BOM 自检节的位置（应在文件末尾的 Step 5/5）。

Use Edit tool — 在 BOM 自检之前插入 chmod 节：

old_string:
```
EOF

# ---- BOM 自检（v2.0.1+）----
```

new_string:
```
EOF

# ---- hooks 可执行（v2.1.0+）----
# hooks/*.sh 必须可执行，Claude Code 才能 invoke
if [ -d "$TARGET/hooks" ]; then
  chmod +x "$TARGET/hooks/"*.sh 2>/dev/null || true
fi

# ---- BOM 自检（v2.0.1+）----
```

- [ ] **Step 2: 验证 bash 语法**

```bash
cd F:/claude/superdev && bash -n mpdev/bin/install.sh && echo "syntax ok"
```

Expected: `syntax ok`

- [ ] **Step 3: 验证 grep**

```bash
cd F:/claude/superdev && grep -c "chmod +x.*hooks" mpdev/bin/install.sh
```

Expected: `1`

- [ ] **Step 4: Commit**

```bash
cd F:/claude/superdev && git add mpdev/bin/install.sh && git commit -m "feat(install.sh v2.1.0): chmod +x hooks/*.sh after copy

v2.1.0 引入 hooks/，install 拷贝后必须给 *.sh 加可执行位，
否则 Claude Code 调 hook 时报 permission denied。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 8: install.ps1 加 .sh 文件 CRLF→LF 处理

**Files:**
- Modify: `mpdev/bin/install.ps1`

- [ ] **Step 1: 在 BOM 自检之前加 CRLF→LF 转换**

Read `F:/claude/superdev/mpdev/bin/install.ps1` 末尾确认 BOM 自检的位置。

Use Edit tool 在 BOM 自检之前插入：

old_string:
```
# BOM 自检（v2.0.1+）
```

new_string:
```
# hooks .sh 文件 LF 行尾保护（v2.1.0+）
# Windows git autocrlf 可能把 .sh 转 CRLF，bash 解析含 shebang 的 .sh 会失败
if (Test-Path (Join-Path $Target 'hooks')) {
    foreach ($f in (Get-ChildItem -Path (Join-Path $Target 'hooks') -Filter '*.sh').FullName) {
        $content = [System.IO.File]::ReadAllText($f) -replace "`r`n", "`n"
        [System.IO.File]::WriteAllText($f, $content, (New-Object System.Text.UTF8Encoding $false))
    }
}

# BOM 自检（v2.0.1+）
```

- [ ] **Step 2: 验证 PowerShell 语法（如有 pwsh）**

```bash
cd F:/claude/superdev && pwsh -NoProfile -Command "try { [scriptblock]::Create((Get-Content 'mpdev/bin/install.ps1' -Raw)) | Out-Null; 'syntax ok' } catch { Write-Host \$_.Exception.Message; exit 1 }" 2>/dev/null || echo "(pwsh skipped)"
```

Expected: `syntax ok` (或 `(pwsh skipped)`)

- [ ] **Step 3: 确认 install.ps1 BOM 仍在**

```bash
cd F:/claude/superdev && head -c 3 mpdev/bin/install.ps1 | od -An -tx1 | tr -d ' \n'
```

Expected: `efbbbf`

如果 BOM 丢了：

```bash
cd F:/claude/superdev && tmp=$(mktemp) && printf '\xef\xbb\xbf' > $tmp && cat mpdev/bin/install.ps1 >> $tmp && mv $tmp mpdev/bin/install.ps1
```

- [ ] **Step 4: Commit**

```bash
cd F:/claude/superdev && git add mpdev/bin/install.ps1 && git commit -m "feat(install.ps1 v2.1.0): convert hooks/*.sh CRLF to LF

Windows git autocrlf 可能把 .sh 转 CRLF，bash 解析 #!/usr/bin/env bash
\\r\\n 时会失败。install.ps1 末尾扫 hooks/*.sh 强制 LF 行尾。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 9: CHANGELOG 加 [2.1.0] 段

**Files:**
- Modify: `mpdev/CHANGELOG.md`

- [ ] **Step 1: 在 [2.0.1] 之前插入 [2.1.0] 段**

Use Edit tool on `mpdev/CHANGELOG.md`:

old_string:
```
## [2.0.1] — 2026-05-18
```

new_string:
```
## [2.1.0] — 2026-05-18

### Added
- **3 个 hooks**（位于 `${CLAUDE_PLUGIN_ROOT}/hooks/`）:
  - **SessionStart** (`hooks/session-start.sh`): 启动时自动检测项目里所有 CLAUDE.md，提取「## 技术栈」节摘要注入 Claude 上下文。省去手工说明项目背景。
  - **UserPromptSubmit** (`hooks/inject-keyword-hint.sh`): 用户输入含 7 种模式关键词时（"修 bug" / "新需求" / "理解项目" / "启动服务" / "写测试" / "检查契约" / "生成 commit"），systemMessage 提示对应 `/mpdev:*` 命令。
  - **SubagentStop** (`hooks/post-subagent-check.sh`): impl agent 输出 `cross_module_issue` 字段非 null 时，additionalContext 提示跨模块影响 + 启发式抽出目标模块名建议下一步。
- **`MPDEV_NO_HOOKS=1` 一开关**全禁所有 hook（适合 CI / 噪声敏感场景）
- `hooks/_lib.sh` 共享工具（禁用检查 / 项目根定位 / YAML 解析）
- `docs/troubleshooting.md` 新增 hook 章节

### Changed
- `plugin.json` 新增 `"hooks": "./hooks/hooks.json"` 字段
- `install.sh` 末尾 chmod +x hooks/*.sh
- `install.ps1` 末尾把 .sh 行尾从 CRLF 转 LF（防 Windows git autocrlf 引入 \r\n 破坏 shebang）

### Notes
- hook 脚本为 bash；Windows 用户需 Git Bash for Windows
- v2.0.x 用户 `/plugin update` 直接拉到 v2.1.0
- 3 个 hook 全 fail-silent + 3-5 秒超时
- hooks 全部只读（不写项目数据）

### 未做（推迟到 v2.2+）
- PostToolUse(git commit) 自动归档 — 与 /mpdev:commit 重叠分析未完成
- PreToolUse(git push) 调 /mpdev:check — 高侵入，需 opt-in 设计
- PostToolUse(Edit/Write) doc-refresher — 需 batch 机制
- PowerShell 原生 hook 支持（.ps1）
- 项目级 `.claude/.mpdev-hooks.yml` 细粒度开关

## [2.0.1] — 2026-05-18
```

- [ ] **Step 2: 验证**

```bash
cd F:/claude/superdev && grep -c "## \[2.1.0\]" mpdev/CHANGELOG.md && grep -c "## \[2.0.1\]" mpdev/CHANGELOG.md
```

Expected: 1 / 1

- [ ] **Step 3: Commit**

```bash
cd F:/claude/superdev && git add mpdev/CHANGELOG.md && git commit -m "docs(changelog): add [2.1.0] hooks section

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 10: troubleshooting.md 加 Hooks 节

**Files:**
- Modify: `mpdev/docs/troubleshooting.md`

- [ ] **Step 1: 找 troubleshooting.md 末尾**

Read `F:/claude/superdev/mpdev/docs/troubleshooting.md` 末尾几行。

- [ ] **Step 2: 在末尾追加 "Hooks 故障排查" 节**

Use Edit tool — 找到文件末尾最后一节标题（例如 `## 还有其他问题`）：

old_string:
```
## 还有其他问题

提 issue 时附：

```bash
# 收集诊断信息
cd ~/.claude/plugins/cache/mpdev/mpdev && cat VERSION
ls commands/
/plugin list | grep mpdev
```
```

new_string:
```
## Hooks 故障排查（v2.1.0+）

### Hook 不生效

可能原因：

1. **plugin 装的是 v2.0.x 版本，没 hooks**
   ```bash
   cat ~/.claude/plugins/cache/mpdev/mpdev/VERSION
   ```
   显示 < 2.1.0 → `/plugin update` 拉新版

2. **plugin.json 未注册 hooks**
   ```bash
   grep hooks ~/.claude/plugins/cache/mpdev/mpdev/.claude-plugin/plugin.json
   ```
   应见 `"hooks": "./hooks/hooks.json"`。缺失 → 重装 plugin

3. **hooks/*.sh 不可执行**（仅 Linux/Mac）
   ```bash
   ls -l ~/.claude/plugins/cache/mpdev/mpdev/hooks/
   ```
   `*.sh` 应有 `x` 权限位。缺失 → 重跑 install.sh

4. **Windows 用了 cmd.exe / PowerShell 而非 Git Bash**
   v2.1.0 hooks 都是 bash 脚本。Windows 必须装 Git for Windows，Claude Code 会用 Git Bash 执行 hook。如未装 → 装 [Git for Windows](https://gitforwindows.org/)

5. **hook 静默失败**
   bash hook 设计成 fail-silent。临时调试：
   ```bash
   bash -x ~/.claude/plugins/cache/mpdev/mpdev/hooks/session-start.sh < /dev/null
   ```
   查找 `cannot execute` / `command not found` 等错误。

### Hook 太吵 / 不想要

```bash
# 一开关全禁（当前 shell 生效）
export MPDEV_NO_HOOKS=1

# 永久禁用（写入 ~/.bashrc 或 PowerShell profile）
echo 'export MPDEV_NO_HOOKS=1' >> ~/.bashrc
```

或卸载 plugin：`/plugin uninstall mpdev`

### Hook 报错"jq: command not found"

hooks 优先用 jq；缺失会退化到 grep/sed 但可能不够准。装 jq：

```bash
# macOS:   brew install jq
# Ubuntu:  sudo apt install jq
# Windows Git Bash: 通常自带
```

### SessionStart hook 注入的项目类型不对

hook 扫的是项目里**所有** CLAUDE.md。如果版本太旧/不准 → `/mpdev:understand` 刷新各模块 CLAUDE.md。

如果 hook 注入了不该有的模块（如已删除的子项目目录）→ 删 `.claude/agents/<X>-impl.md` 或重跑 `/mpdev:init`。

### Windows .sh 文件 CRLF 行尾报错

Windows git autocrlf 可能把 .sh 转 CRLF，bash 报 `\r: command not found`。修复：

```bash
cd ~/.claude/plugins/cache/mpdev/mpdev
dos2unix hooks/*.sh
# 或者
for f in hooks/*.sh; do sed -i 's/\r$//' "$f"; done
```

v2.1.0 install.ps1 自动处理这步；如果你跑的是 install.sh（Linux/Mac/Git Bash）不会出现这个问题。

---

## 还有其他问题

提 issue 时附：

```bash
# 收集诊断信息
cd ~/.claude/plugins/cache/mpdev/mpdev && cat VERSION
ls commands/
ls hooks/    # v2.1.0+
/plugin list | grep mpdev
```
```

- [ ] **Step 3: 验证**

```bash
cd F:/claude/superdev && grep -c "Hooks 故障排查" mpdev/docs/troubleshooting.md
```

Expected: 1

- [ ] **Step 4: Commit**

```bash
cd F:/claude/superdev && git add mpdev/docs/troubleshooting.md && git commit -m "docs(troubleshooting): add Hooks section (v2.1.0)

7 种故障场景：plugin 版本不对 / 未注册 hooks / .sh 不可执行 /
Windows 没 Git Bash / hook 静默失败 / 太吵 / jq 不存在 / SessionStart
项目类型不对 / Windows CRLF 行尾。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 11: mpdev/README.md 加 hooks 章节

**Files:**
- Modify: `mpdev/README.md`

- [ ] **Step 1: 找 "9 个命令" 节末尾**

Read `F:/claude/superdev/mpdev/README.md` 找 "9 个命令" 表格 + 数据架构图位置。

- [ ] **Step 2: 在 "9 个命令" 节后插入 "3 个 hooks" 节**

Use Edit tool — 找一个 "9 个命令" 节之后、"## 数据架构" 之前的稳定 anchor：

old_string:
```
| `/mpdev:commit` | 智能 commit 消息生成 | 提交前 |

---

## 数据架构
```

new_string:
```
| `/mpdev:commit` | 智能 commit 消息生成 | 提交前 |

---

## 3 个 Hooks（v2.1.0+）

| Hook 事件 | 触发 | 作用 |
|----------|------|------|
| `SessionStart` | Claude Code 启动 | 扫项目 CLAUDE.md → 注入技术栈摘要给 Claude（用户不可见） |
| `UserPromptSubmit` | 用户提交 prompt | 含 "修 bug / 新需求 / 启动服务" 等关键词时，提示对应 `/mpdev:*` 命令 |
| `SubagentStop` | subagent 跑完 | impl agent 报 `cross_module_issue` 时，提示下一步 `/mpdev:fix <module>` |

**禁用**：`export MPDEV_NO_HOOKS=1` 一开关全禁。

**跨平台**：bash only；Windows 需 Git Bash for Windows。

---

## 数据架构
```

- [ ] **Step 3: 验证**

```bash
cd F:/claude/superdev && grep -c "## 3 个 Hooks" mpdev/README.md
```

Expected: 1

- [ ] **Step 4: Commit**

```bash
cd F:/claude/superdev && git add mpdev/README.md && git commit -m "docs(readme): add Hooks section to mpdev plugin README

3 个 hooks 表格 + MPDEV_NO_HOOKS 禁用说明 + Windows Git Bash 提示。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 12: 顶层 README badge 版本

**Files:**
- Modify: `README.md`

- [ ] **Step 1: 改 mpdev badge 版本**

Use Edit tool on `F:/claude/superdev/README.md`:
- old_string: `[![mpdev version](https://img.shields.io/badge/mpdev-v2.0.1-blue)](./mpdev/)`
- new_string: `[![mpdev version](https://img.shields.io/badge/mpdev-v2.1.0-blue)](./mpdev/)`

- [ ] **Step 2: 验证**

```bash
cd F:/claude/superdev && grep -c "mpdev-v2.1.0" README.md && grep -c "mpdev-v2.0.1" README.md
```

Expected: 1 / 0

- [ ] **Step 3: Commit**

```bash
cd F:/claude/superdev && git add README.md && git commit -m "docs(readme): bump mpdev badge to v2.1.0

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 13: 验收剧本 #1-3 — SessionStart hook

**Files:** 无修改

- [ ] **Step 1: 测试 — 空目录**

```bash
cd F:/claude/superdev && \
  rm -rf /tmp/v210-t13-empty && mkdir -p /tmp/v210-t13-empty && \
  cd /tmp/v210-t13-empty && \
  echo '{}' | bash F:/claude/superdev/mpdev/hooks/session-start.sh
```

Expected: `{}`

- [ ] **Step 2: 测试 — 单 CLAUDE.md**

```bash
cd F:/claude/superdev && \
  rm -rf /tmp/v210-t13-single && mkdir -p /tmp/v210-t13-single/java && \
  cat > /tmp/v210-t13-single/java/CLAUDE.md << 'EOF'
# Java 后端

## 技术栈

- Java 1.8
- Spring Boot 2.3.12

## 目录结构

src/main/java/...
EOF
  cd /tmp/v210-t13-single && \
  echo '{}' | bash F:/claude/superdev/mpdev/hooks/session-start.sh | python -c "import sys,json; r=json.load(sys.stdin); print('event:', r['hookSpecificOutput']['hookEventName']); print('ctx has tech:', 'Spring Boot' in r['hookSpecificOutput']['additionalContext'])"
```

Expected:
```
event: SessionStart
ctx has tech: True
```

- [ ] **Step 3: 测试 — 多 CLAUDE.md (含 > 5 截断)**

```bash
cd F:/claude/superdev && \
  rm -rf /tmp/v210-t13-many && \
  for m in java vue dispatch analytics algor pad h5; do
    mkdir -p /tmp/v210-t13-many/$m
    cat > /tmp/v210-t13-many/$m/CLAUDE.md << EOF
# $m

## 技术栈

- $m tech stack
EOF
  done && \
  cd /tmp/v210-t13-many && \
  echo '{}' | bash F:/claude/superdev/mpdev/hooks/session-start.sh | python -c "import sys,json; r=json.load(sys.stdin); ctx=r['hookSpecificOutput']['additionalContext']; print('5 模块?', '检测到 5 个' in ctx); print('提示剩余?', '还有 2 个 CLAUDE.md 未注入' in ctx)"
```

Expected: `5 模块? True` / `提示剩余? True`

- [ ] **Step 4: 清理**

```bash
rm -rf /tmp/v210-t13-empty /tmp/v210-t13-single /tmp/v210-t13-many
```

---

## Task 14: 验收剧本 #4-5 — UserPromptSubmit hook

**Files:** 无修改

- [ ] **Step 1: 测试 — 命中"修 bug"**

```bash
cd F:/claude/superdev && \
  echo '{"prompt":"帮我修个 NPE bug"}' | bash mpdev/hooks/inject-keyword-hint.sh | python -c "import sys,json; r=json.load(sys.stdin); msg=r['hookSpecificOutput']['systemMessage']; print('event:', r['hookSpecificOutput']['hookEventName']); print('mentions /mpdev:fix:', '/mpdev:fix' in msg)"
```

Expected:
```
event: UserPromptSubmit
mentions /mpdev:fix: True
```

- [ ] **Step 2: 测试 — prompt 以 /mpdev: 开头（防自我推荐）**

```bash
cd F:/claude/superdev && \
  echo '{"prompt":"/mpdev:fix vue 下拉框 bug"}' | bash mpdev/hooks/inject-keyword-hint.sh
```

Expected: `{}`

- [ ] **Step 3: 测试 — 其他 6 个关键词模式**

```bash
cd F:/claude/superdev && \
  for prompt_test in \
    "我有个新需求要加 night_patrol 任务|dev" \
    "理解一下这个新项目|understand" \
    "启动服务环境|env" \
    "帮我写测试用例|test" \
    "检查跨模块契约一致性|check" \
    "生成 commit 提交说明|commit"; do
    p="${prompt_test%|*}"
    expected_cmd="${prompt_test##*|}"
    result=$(echo "{\"prompt\":\"$p\"}" | bash mpdev/hooks/inject-keyword-hint.sh)
    if echo "$result" | grep -q "/mpdev:$expected_cmd"; then
      echo "  ✅ '$p' → /mpdev:$expected_cmd"
    else
      echo "  ❌ '$p' expected /mpdev:$expected_cmd, got: $result"
    fi
  done
```

Expected: 6 行全部 ✅

- [ ] **Step 4: 测试 — 无关 prompt**

```bash
cd F:/claude/superdev && \
  echo '{"prompt":"今天天气怎么样"}' | bash mpdev/hooks/inject-keyword-hint.sh
```

Expected: `{}`

---

## Task 15: 验收剧本 #6 — MPDEV_NO_HOOKS=1 总开关

**Files:** 无修改

- [ ] **Step 1: 3 个 hook 都跑（带 MPDEV_NO_HOOKS=1）**

```bash
cd F:/claude/superdev && \
  echo "=== session-start.sh ===" && \
  echo '{}' | MPDEV_NO_HOOKS=1 bash mpdev/hooks/session-start.sh && \
  echo "" && \
  echo "=== inject-keyword-hint.sh ===" && \
  echo '{"prompt":"帮我修 bug"}' | MPDEV_NO_HOOKS=1 bash mpdev/hooks/inject-keyword-hint.sh && \
  echo "" && \
  echo "=== post-subagent-check.sh ===" && \
  echo '{"subagent_response":"cross_module_issue: java 也要改"}' | MPDEV_NO_HOOKS=1 bash mpdev/hooks/post-subagent-check.sh
```

Expected: 3 个 hook 全输出 `{}`

---

## Task 16: 验收剧本 #7 — SubagentStop hook

**Files:** 无修改

- [ ] **Step 1: 测试 — cross_module_issue 含模块名**

```bash
cd F:/claude/superdev && \
  cat << 'EOF' | bash mpdev/hooks/post-subagent-check.sh | python -c "import sys,json; r=json.load(sys.stdin); ctx=r['hookSpecificOutput']['additionalContext']; print('event:', r['hookSpecificOutput']['hookEventName']); print('mentions /mpdev:fix dispatch:', '/mpdev:fix dispatch' in ctx)"
{"subagent_response": "status: ok\ncross_module_issue: 修 java NPE 时发现 dispatch 模块也需要补 null 检查"}
EOF
```

Expected:
```
event: SubagentStop
mentions /mpdev:fix dispatch: True
```

- [ ] **Step 2: 测试 — cross_module_issue: null**

```bash
cd F:/claude/superdev && \
  echo '{"subagent_response":"cross_module_issue: null"}' | bash mpdev/hooks/post-subagent-check.sh
```

Expected: `{}`

- [ ] **Step 3: 测试 — 无 cross_module_issue 字段**

```bash
cd F:/claude/superdev && \
  echo '{"subagent_response":"status: fixed\nfiles_changed: src/X.java"}' | bash mpdev/hooks/post-subagent-check.sh
```

Expected: `{}`

- [ ] **Step 4: 测试 — 兼容 .output 字段**

```bash
cd F:/claude/superdev && \
  echo '{"output":"cross_module_issue: vue 也要改"}' | bash mpdev/hooks/post-subagent-check.sh | grep -c "/mpdev:fix vue"
```

Expected: `1`

- [ ] **Step 5: 测试 — 启发式抽不到模块名**

```bash
cd F:/claude/superdev && \
  echo '{"subagent_response":"cross_module_issue: 另一服务也要改"}' | bash mpdev/hooks/post-subagent-check.sh | grep -c "建议检查 cross_module_issue 涉及的模块"
```

Expected: `1`

---

## Task 17: 综合 git log + 自查

**Files:** 无修改

- [ ] **Step 1: 综合 git log**

```bash
cd F:/claude/superdev && git log --oneline 152d592..HEAD
```

Expected: ~12 个 commit（T1-T12 各一个）

- [ ] **Step 2: diff stat**

```bash
cd F:/claude/superdev && git diff --stat 152d592..HEAD | tail -3
```

Expected: ~14 文件 / ~315 行新增 / 极少删除

- [ ] **Step 3: 验证 v2.1.0 标识**

```bash
cd F:/claude/superdev && \
  echo "VERSION:" && cat mpdev/VERSION && echo "" && \
  echo "plugin.json:" && grep '"version"\|"hooks"' mpdev/.claude-plugin/plugin.json && \
  echo "marketplace.json:" && grep '"version"' mpdev/.claude-plugin/marketplace.json && \
  echo "CHANGELOG:" && head -25 mpdev/CHANGELOG.md | grep "\[2.1.0\]" && \
  echo "README badge:" && grep "mpdev-v2.1.0" README.md | head -1
```

Expected: 全部输出 2.1.0 + plugin.json 含 `"hooks": "./hooks/hooks.json"`

- [ ] **Step 4: 验证 hooks 文件清单**

```bash
cd F:/claude/superdev && ls mpdev/hooks/
```

Expected:
```
_lib.sh
hooks.json
inject-keyword-hint.sh
post-subagent-check.sh
session-start.sh
```

- [ ] **Step 5: 验证 install.sh / install.ps1 都加了 hook 处理**

```bash
cd F:/claude/superdev && \
  echo "install.sh chmod:" && grep -c "chmod +x.*hooks" mpdev/bin/install.sh && \
  echo "install.ps1 CRLF→LF:" && grep -c "ReadAllText.*replace.*r.n" mpdev/bin/install.ps1
```

Expected: 1 / 1

- [ ] **Step 6: 残留 v2.0.1 检查**

```bash
cd F:/claude/superdev && grep "2.0.1" mpdev/VERSION mpdev/.claude-plugin/plugin.json mpdev/.claude-plugin/marketplace.json 2>/dev/null && echo "(should be empty)"
```

Expected: 无输出

---

## Self-Review

实施完成后对照 spec [`docs/superpowers/specs/2026-05-18-mpdev-v2.1.0-hooks.md`](../specs/2026-05-18-mpdev-v2.1.0-hooks.md)：

- [ ] §4 文件清单 14 文件全覆盖 → T1-T12 ✓
- [ ] §5 hooks.json 内容 → T1 ✓
- [ ] §6 _lib.sh 工具 → T2 ✓
- [ ] §7 session-start.sh 实现 → T3 + 验收 T13 ✓
- [ ] §8 inject-keyword-hint.sh → T4 + 验收 T14 ✓
- [ ] §9 post-subagent-check.sh → T5 + 验收 T16 ✓
- [ ] §10 plugin.json hooks 字段 + 版本 → T6 ✓
- [ ] §11 install chmod / CRLF → T7 + T8 ✓
- [ ] §12 CHANGELOG → T9 ✓
- [ ] §13 验收剧本 6 项（人工 1 项留给用户实测） → T13/T14/T15/T16 ✓
- [ ] §14 风险（SubagentStop schema 兼容 3 字段名）→ T5 实现 ✓
- [ ] §15 不在范围 → 0 task 涉及 ✓

跨任务一致性：
- 共享工具 `output_additional_context` / `output_system_message` 在 T3/T4/T5 一致调用 ✓
- `MPDEV_NO_HOOKS=1` 检查全部走 `check_disabled` 函数 ✓
- jq 优先 / grep+sed 退化模式 3 hook 一致 ✓
