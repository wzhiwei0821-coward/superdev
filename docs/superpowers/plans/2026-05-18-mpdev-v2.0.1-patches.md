# mpdev v2.0.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v2.0.1 patch — install.sh/.ps1 末尾自动校验/补齐 .ps1 的 UTF-8 BOM；migrate-from-v1.sh/.ps1 加 opt-in `--auto-clean-framework-agents` flag。

**Architecture:** 4 个脚本各加 1 段独立逻辑（install 加 BOM 自检 / migrate 加参数解析 + auto-clean 分支），不动其他流程。所有改动 backward compatible。

**Tech Stack:** Bash + PowerShell + UTF-8 BOM (EF BB BF) + JSON manifest 版本号。

**Spec:** [`docs/superpowers/specs/2026-05-18-mpdev-v2.0.1-patches.md`](../specs/2026-05-18-mpdev-v2.0.1-patches.md)

---

## File Structure

### 修改清单（9 个文件）

| 路径 | 改动 |
|------|------|
| `mpdev/bin/install.sh` | Step 4/4 改 4/5；末尾追加 Step 5/5 BOM 自检 |
| `mpdev/bin/install.ps1` | 末尾追加 BOM 自检 |
| `mpdev/scripts/migrate-from-v1.sh` | 顶部加参数解析；Step 2 末尾加 auto-clean 分支；Step 3 提示文本条件化 |
| `mpdev/scripts/migrate-from-v1.ps1` | 等价改造 |
| `mpdev/VERSION` | `2.0.0` → `2.0.1` |
| `mpdev/.claude-plugin/plugin.json` | `"version": "2.0.0"` → `"version": "2.0.1"` |
| `mpdev/.claude-plugin/marketplace.json` | `"version": "2.0.0"` → `"version": "2.0.1"` |
| `mpdev/CHANGELOG.md` | 顶部加 `## [2.0.1] — 2026-05-18` 段 |
| `README.md` | mpdev badge `v2.0.0` → `v2.0.1` |

---

## Task 1: install.sh 加 BOM 自检（Step 5/5）

**Files:**
- Modify: `mpdev/bin/install.sh`

- [ ] **Step 1: 把 Step 4/4 改为 Step 4/5**

Use Edit tool:
- old_string: `echo "▶ Step 4/4: 在 Claude Code 内完成 plugin 注册"`
- new_string: `echo "▶ Step 4/5: 在 Claude Code 内完成 plugin 注册"`

- [ ] **Step 2: 在文件末尾、`exit 0` 之前插入 BOM 自检**

Use Edit tool on `mpdev/bin/install.sh`:

old_string:
```
EOF

echo "✅ 安装完成"
exit 0
```

new_string:
```
EOF

# ---- BOM 自检（v2.0.1+）----
# 修复 T42 audit bug：中文 Windows 上无 BOM 的 .ps1 会被 GBK 解码 → 整个文件挂
echo ""
echo "▶ Step 5/5: 校验 .ps1 文件 BOM"
fixed=0
# 用 find -print0 + while read -d '' 处理路径含空格的情况
while IFS= read -r -d '' f; do
  # 读前 3 字节
  bom=$(head -c 3 "$f" | od -An -tx1 | tr -d ' \n')
  if [ "$bom" != "efbbbf" ]; then
    # 缺 BOM，补上：写 EF BB BF + 原内容
    tmp="$(mktemp)"
    printf '\xef\xbb\xbf' > "$tmp"
    cat "$f" >> "$tmp"
    mv "$tmp" "$f"
    fixed=$((fixed + 1))
  fi
done < <(find "$TARGET" -name '*.ps1' -print0 2>/dev/null)
if [ $fixed -gt 0 ]; then
  echo "  ⚠️  补充了 $fixed 个 .ps1 文件的 UTF-8 BOM（可能源仓未加 BOM）"
else
  echo "  ✅ 所有 .ps1 文件 BOM 完整"
fi

echo "✅ 安装完成"
exit 0
```

- [ ] **Step 3: 验证 bash 语法 + grep**

```bash
cd F:/claude/superdev && \
  bash -n mpdev/bin/install.sh && echo "syntax ok" && \
  grep -c "Step 5/5" mpdev/bin/install.sh && \
  grep -c "Step 4/5" mpdev/bin/install.sh && \
  grep -c "efbbbf" mpdev/bin/install.sh
```

Expected: `syntax ok` / 1 / 1 / 1

- [ ] **Step 4: Commit**

```bash
cd F:/claude/superdev && git add mpdev/bin/install.sh && git commit -m "feat(install.sh): add BOM auto-fix for .ps1 files (Step 5/5)

修复 T42 audit bug：中文 Windows 上无 BOM 的 .ps1 被 GBK 解码导致整个文件挂。
install.sh 末尾扫 mpdev/**/*.ps1，缺 BOM 自动补齐。同时保护 fork 场景。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: install.ps1 加 BOM 自检

**Files:**
- Modify: `mpdev/bin/install.ps1`

- [ ] **Step 1: 末尾追加 BOM 自检逻辑**

Use Edit tool:

old_string:
```
Write-Host "文档:    $Target/docs/quickstart.md"
Write-Host "升级:    $Target/docs/upgrade-guide.md"
Write-Host "排错:    $Target/docs/troubleshooting.md"
```

new_string:
```
Write-Host "文档:    $Target/docs/quickstart.md"
Write-Host "升级:    $Target/docs/upgrade-guide.md"
Write-Host "排错:    $Target/docs/troubleshooting.md"

# BOM 自检（v2.0.1+）
Write-Host ''
$utf8Bom = New-Object System.Text.UTF8Encoding $true
$fixed = 0
foreach ($f in (Get-ChildItem -Path $Target -Recurse -Filter '*.ps1').FullName) {
    $bytes = [System.IO.File]::ReadAllBytes($f) | Select-Object -First 3
    $hasBom = ($bytes.Count -ge 3 -and $bytes[0] -eq 0xEF -and $bytes[1] -eq 0xBB -and $bytes[2] -eq 0xBF)
    if (-not $hasBom) {
        $content = [System.IO.File]::ReadAllText($f, [System.Text.Encoding]::UTF8)
        [System.IO.File]::WriteAllText($f, $content, $utf8Bom)
        $fixed++
    }
}
if ($fixed -gt 0) {
    Write-Host "⚠️ 补充了 $fixed 个 .ps1 文件的 UTF-8 BOM（可能源仓未加 BOM）" -ForegroundColor Yellow
} else {
    Write-Host '✅ 所有 .ps1 文件 BOM 完整' -ForegroundColor Green
}
```

- [ ] **Step 2: 验证 PowerShell 语法**

```bash
cd F:/claude/superdev && pwsh -NoProfile -Command "try { [scriptblock]::Create((Get-Content 'mpdev/bin/install.ps1' -Raw)) | Out-Null; 'syntax ok' } catch { Write-Host \$_.Exception.Message; exit 1 }" 2>/dev/null || echo "(pwsh not available; skipping)"
```

Expected: `syntax ok` if pwsh available; else "(pwsh not available...)"

- [ ] **Step 3: 验证 BOM 自检代码已添加**

```bash
cd F:/claude/superdev && grep -c "UTF8Encoding" mpdev/bin/install.ps1
```

Expected: ≥ 1

- [ ] **Step 4: 确保 install.ps1 自己仍有 BOM（v2.0.0 已加，不要弄掉）**

```bash
cd F:/claude/superdev && head -c 3 mpdev/bin/install.ps1 | od -An -tx1 | tr -d ' \n'
```

Expected: `efbbbf`

如果输出不是 `efbbbf`，需要重新加 BOM：
```bash
cd F:/claude/superdev && tmp=$(mktemp) && printf '\xef\xbb\xbf' > $tmp && cat mpdev/bin/install.ps1 >> $tmp && mv $tmp mpdev/bin/install.ps1
```

- [ ] **Step 5: Commit**

```bash
cd F:/claude/superdev && git add mpdev/bin/install.ps1 && git commit -m "feat(install.ps1): add BOM auto-fix for .ps1 files

PowerShell 等价 install.sh 的 BOM 自检逻辑。装到 ~/dev/mpdev 后扫
所有 .ps1，缺 BOM 自动补 UTF-8 BOM。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: migrate-from-v1.sh 加 --auto-clean-framework-agents flag

**Files:**
- Modify: `mpdev/scripts/migrate-from-v1.sh`

- [ ] **Step 1: 顶部加参数解析**

Use Edit tool:

old_string:
```
set -e

if [ ! -d .claude ]; then
  echo "❌ 当前目录无 .claude/，不像 v1 项目"; exit 1
fi
```

new_string:
```
set -e

# ---- 参数（v2.0.1+）----
AUTO_CLEAN=false
for arg in "$@"; do
  case $arg in
    --auto-clean-framework-agents) AUTO_CLEAN=true; shift ;;
    -h|--help)
      cat <<EOF
Usage: migrate-from-v1.sh [--auto-clean-framework-agents]

参数:
  --auto-clean-framework-agents  额外删除 4 个框架 agent（code-reviewer/
                                  integration-checker/acceptance-reviewer/
                                  doc-refresher），让 plugin 接管。
                                  默认不删（保护项目自定义）。
EOF
      exit 0 ;;
    *) echo "❌ 未知参数: $arg"; exit 1 ;;
  esac
done

if [ ! -d .claude ]; then
  echo "❌ 当前目录无 .claude/，不像 v1 项目"; exit 1
fi
```

- [ ] **Step 2: 在 Step 2/3 末尾加 auto-clean 分支**

Use Edit tool:

old_string:
```
# 删顶级 md 文件
for f in MPDev-Scheme.md mpdev-suite-workflow.md README.md .mpdev-version; do
  if [ -f ".claude/$f" ]; then
    rm -f ".claude/$f"
    echo "  - .claude/$f (plugin 接管)"
  fi
done

echo ""
echo "▶ Step 3/3: 检查保留的项目数据"
```

new_string:
```
# 删顶级 md 文件
for f in MPDev-Scheme.md mpdev-suite-workflow.md README.md .mpdev-version; do
  if [ -f ".claude/$f" ]; then
    rm -f ".claude/$f"
    echo "  - .claude/$f (plugin 接管)"
  fi
done

# 删 4 个框架 agent（仅当 --auto-clean-framework-agents）
if [ "$AUTO_CLEAN" = "true" ]; then
  echo ""
  echo "  --auto-clean-framework-agents: 移除 4 个框架 agent"
  cleaned=0
  for a in code-reviewer integration-checker acceptance-reviewer doc-refresher; do
    if [ -f ".claude/agents/$a.md" ]; then
      rm -f ".claude/agents/$a.md"
      echo "  - .claude/agents/$a.md (让 plugin 接管)"
      cleaned=$((cleaned + 1))
    fi
  done
  echo "  ✅ 已清理 $cleaned 个框架 agent"
fi

echo ""
echo "▶ Step 3/3: 检查保留的项目数据"
```

- [ ] **Step 3: 把 Step 3 提示文本条件化**

Use Edit tool:

old_string:
```
cat <<'EOF'
后续:
  1. 确认 v2 plugin 已装:
     /plugin list | grep mpdev

  2. 推荐删 4 个框架 agent（让 plugin 接管最新版本）:
     rm .claude/agents/code-reviewer.md
     rm .claude/agents/integration-checker.md
     rm .claude/agents/acceptance-reviewer.md
     rm .claude/agents/doc-refresher.md
     （项目特化的 *-impl.md / architect.md / 等不要删）

  3. 完全重启 Claude Code，验证 /mpdev: 自动补全

  4. 确认无问题后可删备份:
     rm -rf "BACKUP_PATH"

  详见 docs/upgrade-guide.md
EOF
```

new_string:
```
if [ "$AUTO_CLEAN" = "true" ]; then
  cat <<'EOF'
后续:
  1. 确认 v2 plugin 已装:
     /plugin list | grep mpdev

  2. ✅ 框架 agent 已被 plugin 接管（已通过 --auto-clean-framework-agents）

  3. 完全重启 Claude Code，验证 /mpdev: 自动补全

  4. 确认无问题后可删备份:
     rm -rf "BACKUP_PATH"

  详见 docs/upgrade-guide.md
EOF
else
  cat <<'EOF'
后续:
  1. 确认 v2 plugin 已装:
     /plugin list | grep mpdev

  2. 推荐删 4 个框架 agent（让 plugin 接管最新版本）:
     rm .claude/agents/code-reviewer.md
     rm .claude/agents/integration-checker.md
     rm .claude/agents/acceptance-reviewer.md
     rm .claude/agents/doc-refresher.md
     （项目特化的 *-impl.md / architect.md / 等不要删）

     💡 或下次跑 migrate 时加 --auto-clean-framework-agents flag 自动清理

  3. 完全重启 Claude Code，验证 /mpdev: 自动补全

  4. 确认无问题后可删备份:
     rm -rf "BACKUP_PATH"

  详见 docs/upgrade-guide.md
EOF
fi
```

- [ ] **Step 4: 验证 bash 语法**

```bash
cd F:/claude/superdev && bash -n mpdev/scripts/migrate-from-v1.sh && echo "syntax ok" && grep -c "auto-clean-framework-agents" mpdev/scripts/migrate-from-v1.sh
```

Expected: `syntax ok` / ≥ 3 (一次在参数 case，一次在 Step 2 分支，一次在条件提示)

- [ ] **Step 5: Commit**

```bash
cd F:/claude/superdev && git add mpdev/scripts/migrate-from-v1.sh && git commit -m "feat(migrate.sh): add --auto-clean-framework-agents flag

opt-in 删除 4 个框架 agent（code-reviewer / integration-checker /
acceptance-reviewer / doc-refresher）让 plugin 接管。默认不删（保护
项目自定义）。Step 3 提示文本根据是否使用 flag 条件化。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: migrate-from-v1.ps1 加 --auto-clean-framework-agents flag

**Files:**
- Modify: `mpdev/scripts/migrate-from-v1.ps1`

- [ ] **Step 1: 顶部加参数解析（在 try/catch 之后、Test-Path '.claude' 之前）**

Use Edit tool:

old_string:
```
try {
    [Console]::OutputEncoding = [System.Text.Encoding]::UTF8
    $OutputEncoding = [System.Text.Encoding]::UTF8
} catch {}

if (-not (Test-Path '.claude')) {
    Write-Host "[ERROR] 当前目录无 .claude/，不像 v1 项目" -ForegroundColor Red
    exit 1
}
```

new_string:
```
try {
    [Console]::OutputEncoding = [System.Text.Encoding]::UTF8
    $OutputEncoding = [System.Text.Encoding]::UTF8
} catch {}

# ---- 参数（v2.0.1+）----
$AutoClean = $false
foreach ($a in $args) {
    switch ($a) {
        '--auto-clean-framework-agents' { $AutoClean = $true }
        '-h' { Write-Host "Usage: migrate-from-v1.ps1 [--auto-clean-framework-agents]`n`n参数:`n  --auto-clean-framework-agents  额外删除 4 个框架 agent，让 plugin 接管。`n                                  默认不删（保护项目自定义）。"; exit 0 }
        '--help' { Write-Host "Usage: migrate-from-v1.ps1 [--auto-clean-framework-agents]`n`n参数:`n  --auto-clean-framework-agents  额外删除 4 个框架 agent，让 plugin 接管。`n                                  默认不删（保护项目自定义）。"; exit 0 }
        default { Write-Host "❌ 未知参数: $a" -ForegroundColor Red; exit 1 }
    }
}

if (-not (Test-Path '.claude')) {
    Write-Host "[ERROR] 当前目录无 .claude/，不像 v1 项目" -ForegroundColor Red
    exit 1
}
```

- [ ] **Step 2: 在 Step 2 末尾（删顶级 md 文件之后）加 auto-clean 分支**

读 `mpdev/scripts/migrate-from-v1.ps1` 找到 `foreach ($f in @('MPDev-Scheme.md',...))` 块，在其之后加：

Use Edit tool:

old_string:
```
foreach ($f in @('MPDev-Scheme.md','mpdev-suite-workflow.md','README.md','.mpdev-version')) {
    $p = Join-Path '.claude' $f
    if (Test-Path $p) {
        Remove-Item -Force $p
        Write-Host "  - .claude/$f (plugin 接管)"
    }
}

Write-Host ''
Write-Host '▶ Step 3/3: 检查保留的项目数据' -ForegroundColor Cyan
```

new_string:
```
foreach ($f in @('MPDev-Scheme.md','mpdev-suite-workflow.md','README.md','.mpdev-version')) {
    $p = Join-Path '.claude' $f
    if (Test-Path $p) {
        Remove-Item -Force $p
        Write-Host "  - .claude/$f (plugin 接管)"
    }
}

# 删 4 个框架 agent（仅当 --auto-clean-framework-agents）
if ($AutoClean) {
    Write-Host ''
    Write-Host '  --auto-clean-framework-agents: 移除 4 个框架 agent' -ForegroundColor Cyan
    $cleaned = 0
    foreach ($a in 'code-reviewer','integration-checker','acceptance-reviewer','doc-refresher') {
        $p = ".claude/agents/$a.md"
        if (Test-Path $p) {
            Remove-Item -Force $p
            Write-Host "  - $p (让 plugin 接管)"
            $cleaned++
        }
    }
    Write-Host "  ✅ 已清理 $cleaned 个框架 agent" -ForegroundColor Green
}

Write-Host ''
Write-Host '▶ Step 3/3: 检查保留的项目数据' -ForegroundColor Cyan
```

- [ ] **Step 3: 条件化 Step 3 末尾的 here-string 提示**

Use Edit tool:

old_string:
```
Write-Host @"
后续:
  1. 确认 v2 plugin 已装:
     /plugin list | grep mpdev
  
  2. 推荐删 4 个框架 agent（让 plugin 接管最新版本）:
     Remove-Item .claude/agents/code-reviewer.md, .claude/agents/integration-checker.md, .claude/agents/acceptance-reviewer.md, .claude/agents/doc-refresher.md
     （项目特化的 *-impl.md / architect.md / 等不要删）
  
  3. 完全重启 Claude Code，验证 /mpdev: 自动补全
  
  4. 确认无问题后可删备份:
     Remove-Item -Recurse -Force $Backup
"@
```

new_string:
```
if ($AutoClean) {
    Write-Host @"
后续:
  1. 确认 v2 plugin 已装:
     /plugin list | grep mpdev
  
  2. ✅ 框架 agent 已被 plugin 接管（已通过 --auto-clean-framework-agents）
  
  3. 完全重启 Claude Code，验证 /mpdev: 自动补全
  
  4. 确认无问题后可删备份:
     Remove-Item -Recurse -Force $Backup
"@
} else {
    Write-Host @"
后续:
  1. 确认 v2 plugin 已装:
     /plugin list | grep mpdev
  
  2. 推荐删 4 个框架 agent（让 plugin 接管最新版本）:
     Remove-Item .claude/agents/code-reviewer.md, .claude/agents/integration-checker.md, .claude/agents/acceptance-reviewer.md, .claude/agents/doc-refresher.md
     （项目特化的 *-impl.md / architect.md / 等不要删）

     💡 或下次跑 migrate 时加 --auto-clean-framework-agents flag 自动清理
  
  3. 完全重启 Claude Code，验证 /mpdev: 自动补全
  
  4. 确认无问题后可删备份:
     Remove-Item -Recurse -Force $Backup
"@
}
```

- [ ] **Step 4: 验证 PowerShell 语法 + 确保 BOM**

```bash
cd F:/claude/superdev && \
  head -c 3 mpdev/scripts/migrate-from-v1.ps1 | od -An -tx1 | tr -d ' \n' && echo "" && \
  grep -c "auto-clean-framework-agents" mpdev/scripts/migrate-from-v1.ps1
```

Expected: `efbbbf` 第一行 / ≥ 3 第二行

如果 BOM 没了：
```bash
cd F:/claude/superdev && tmp=$(mktemp) && printf '\xef\xbb\xbf' > $tmp && cat mpdev/scripts/migrate-from-v1.ps1 >> $tmp && mv $tmp mpdev/scripts/migrate-from-v1.ps1
```

- [ ] **Step 5: Commit**

```bash
cd F:/claude/superdev && git add mpdev/scripts/migrate-from-v1.ps1 && git commit -m "feat(migrate.ps1): add --auto-clean-framework-agents flag

PowerShell 等价 migrate.sh 的 opt-in flag 实现。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: 版本号 bump 到 2.0.1

**Files:**
- Modify: `mpdev/VERSION`
- Modify: `mpdev/.claude-plugin/plugin.json`
- Modify: `mpdev/.claude-plugin/marketplace.json`

- [ ] **Step 1: 改 VERSION**

Use Edit tool on `mpdev/VERSION`:
- old_string: `2.0.0`
- new_string: `2.0.1`

(VERSION 是单行文件无尾换行；保留原格式)

- [ ] **Step 2: 改 plugin.json**

Use Edit tool on `mpdev/.claude-plugin/plugin.json`:
- old_string: `"version": "2.0.0",`
- new_string: `"version": "2.0.1",`

- [ ] **Step 3: 改 marketplace.json**

Use Edit tool on `mpdev/.claude-plugin/marketplace.json`:
- old_string: `"version": "2.0.0"`
- new_string: `"version": "2.0.1"`

(marketplace.json 该字段在 metadata 下，可能无 `,` 结尾——按文件实际格式匹配)

- [ ] **Step 4: 验证**

```bash
cd F:/claude/superdev && \
  cat mpdev/VERSION && \
  echo "" && \
  python -c "import json,sys; sys.stdout.reconfigure(encoding='utf-8'); p=json.load(open('mpdev/.claude-plugin/plugin.json',encoding='utf-8')); m=json.load(open('mpdev/.claude-plugin/marketplace.json',encoding='utf-8')); print('plugin.json version:', p['version']); print('marketplace metadata version:', m['metadata']['version'])"
```

Expected: 
```
2.0.1
plugin.json version: 2.0.1
marketplace metadata version: 2.0.1
```

- [ ] **Step 5: Commit**

```bash
cd F:/claude/superdev && git add mpdev/VERSION mpdev/.claude-plugin/plugin.json mpdev/.claude-plugin/marketplace.json && git commit -m "release: bump VERSION + manifests to 2.0.1

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: CHANGELOG 加 [2.0.1] 段

**Files:**
- Modify: `mpdev/CHANGELOG.md`

- [ ] **Step 1: 在 `## [2.0.0]` 之前插入 [2.0.1] 段**

Use Edit tool on `mpdev/CHANGELOG.md`:

old_string:
```
## [2.0.0] — 2026-05-15
```

new_string:
```
## [2.0.1] — 2026-05-18

### Fixed
- **install BOM 自检**：T42 audit 发现的 bug — 中文 Windows 上无 BOM 的 .ps1 被 GBK 解码 → migrate 等脚本完全无法运行。install.sh / install.ps1 末尾扫描 mpdev/**/*.ps1，缺 BOM 自动补齐。同时保护 fork 场景（同事用无 BOM 编辑器改 .ps1 后 push 回仓的情况）。

### Added
- **`--auto-clean-framework-agents` flag**（migrate-from-v1.sh + .ps1）：opt-in 删除 4 个框架 agent（code-reviewer / integration-checker / acceptance-reviewer / doc-refresher）让 plugin 接管。默认不删（保护项目自定义）。
- migrate 脚本支持 `--help` / `-h` 输出 usage。

### Notes
- v2.0.0 用户无需 migrate，直接 `/plugin update` 即可。
- semver patch：完全向后兼容，未改任何命令行为或文件格式。

## [2.0.0] — 2026-05-15
```

- [ ] **Step 2: 验证**

```bash
cd F:/claude/superdev && grep -c "## \[2.0.1\]" mpdev/CHANGELOG.md && grep -c "## \[2.0.0\]" mpdev/CHANGELOG.md
```

Expected: 1 / 1

- [ ] **Step 3: Commit**

```bash
cd F:/claude/superdev && git add mpdev/CHANGELOG.md && git commit -m "docs(changelog): add [2.0.1] section

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 7: 顶层 README badge 更新

**Files:**
- Modify: `README.md`

- [ ] **Step 1: 改 mpdev badge 版本**

Use Edit tool on `README.md`:
- old_string: `[![mpdev version](https://img.shields.io/badge/mpdev-v2.0.0-blue)](./mpdev/)`
- new_string: `[![mpdev version](https://img.shields.io/badge/mpdev-v2.0.1-blue)](./mpdev/)`

- [ ] **Step 2: 验证**

```bash
cd F:/claude/superdev && grep -c "mpdev-v2.0.1" README.md && grep -c "mpdev-v2.0.0" README.md
```

Expected: 1 / 0

- [ ] **Step 3: Commit**

```bash
cd F:/claude/superdev && git add README.md && git commit -m "docs(readme): bump mpdev badge to v2.0.1

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 8: 验收剧本 #1 — install.sh BOM 自检（不需补）

**Files:** 无修改

- [ ] **Step 1: 准备沙盒（确保 mpdev/ 所有 .ps1 已有 BOM）**

```bash
cd F:/claude/superdev && \
  for f in $(find mpdev -name '*.ps1'); do
    head -c 3 $f | od -An -tx1 | tr -d ' \n' | grep -q efbbbf || { echo "MISSING BOM: $f"; exit 1; }
  done && echo "✅ 所有 .ps1 已有 BOM"
```

Expected: `✅ 所有 .ps1 已有 BOM`

- [ ] **Step 2: 跑 install.sh（用本地源）**

```bash
cd F:/claude/superdev && \
  rm -rf /tmp/mpdev-v2.0.1-test1 && \
  MPDEV_TARGET=/tmp/mpdev-v2.0.1-test1 MPDEV_REPO=file://F:/claude/superdev/.git bash mpdev/bin/install.sh 2>&1 | tail -15
```

Expected: 末尾 5 行附近含 `▶ Step 5/5: 校验 .ps1 文件 BOM` 和 `✅ 所有 .ps1 文件 BOM 完整`

- [ ] **Step 3: 验证目标目录 .ps1 BOM**

```bash
cd /tmp/mpdev-v2.0.1-test1 && \
  for f in $(find . -name '*.ps1'); do
    head -c 3 $f | od -An -tx1 | tr -d ' \n' | grep -q efbbbf && echo "  ✅ $f"
  done
```

Expected: 2 行（install.ps1 + migrate-from-v1.ps1）都 ✅

- [ ] **Step 4: 清理**

```bash
rm -rf /tmp/mpdev-v2.0.1-test1
```

---

## Task 9: 验收剧本 #2 — install.sh BOM 自检（需补）

**Files:** 无修改

- [ ] **Step 1: 准备沙盒并故意删除一个 .ps1 的 BOM**

```bash
cd F:/claude/superdev && \
  rm -rf /tmp/mpdev-v2.0.1-test2 && \
  cp -r mpdev /tmp/mpdev-v2.0.1-test2 && \
  # 删除 migrate-from-v1.ps1 的前 3 字节（BOM）
  tail -c +4 /tmp/mpdev-v2.0.1-test2/scripts/migrate-from-v1.ps1 > /tmp/no-bom.ps1 && \
  mv /tmp/no-bom.ps1 /tmp/mpdev-v2.0.1-test2/scripts/migrate-from-v1.ps1 && \
  # 验证 BOM 已删
  head -c 3 /tmp/mpdev-v2.0.1-test2/scripts/migrate-from-v1.ps1 | od -An -tx1 | tr -d ' \n' && echo " (前 3 字节，应非 efbbbf)"
```

Expected: 输出**非** `efbbbf` 的 hex

- [ ] **Step 2: 跑 install.sh 末尾 BOM 自检逻辑（直接调脚本片段，避免重新 clone）**

```bash
cd /tmp/mpdev-v2.0.1-test2 && TARGET=/tmp/mpdev-v2.0.1-test2 && \
  fixed=0 && \
  while IFS= read -r -d '' f; do
    bom=$(head -c 3 "$f" | od -An -tx1 | tr -d ' \n')
    if [ "$bom" != "efbbbf" ]; then
      tmp="$(mktemp)" && printf '\xef\xbb\xbf' > "$tmp" && cat "$f" >> "$tmp" && mv "$tmp" "$f" && \
      fixed=$((fixed + 1))
    fi
  done < <(find "$TARGET" -name '*.ps1' -print0 2>/dev/null) && \
  echo "fixed: $fixed (expect 1)"
```

Expected: `fixed: 1`

- [ ] **Step 3: 验证 BOM 已被补回**

```bash
head -c 3 /tmp/mpdev-v2.0.1-test2/scripts/migrate-from-v1.ps1 | od -An -tx1 | tr -d ' \n' && echo " (应为 efbbbf)"
```

Expected: `efbbbf` 

- [ ] **Step 4: 清理**

```bash
rm -rf /tmp/mpdev-v2.0.1-test2
```

---

## Task 10: 验收剧本 #3 — install.ps1 BOM 自检（Windows PowerShell）

**Files:** 无修改

- [ ] **Step 1: 准备沙盒**

在 PowerShell 中执行：

```powershell
$sb = "$env:TEMP\mpdev-v2.0.1-test3"
Remove-Item -Recurse -Force $sb -ErrorAction SilentlyContinue
Copy-Item -Recurse F:\claude\superdev\mpdev $sb

# 故意删除 migrate-from-v1.ps1 的 BOM
$f = "$sb\scripts\migrate-from-v1.ps1"
$bytes = [System.IO.File]::ReadAllBytes($f)
[System.IO.File]::WriteAllBytes($f, $bytes[3..($bytes.Count-1)])
# 验证 BOM 已删
$b = [System.IO.File]::ReadAllBytes($f) | Select-Object -First 3
Write-Host "BOM bytes: $($b | ForEach-Object { '0x{0:X2}' -f $_ })"
```

Expected: BOM bytes 非 `0xEF 0xBB 0xBF`

- [ ] **Step 2: 跑 install.ps1 末尾 BOM 自检（直接调代码片段）**

```powershell
$Target = "$env:TEMP\mpdev-v2.0.1-test3"
$utf8Bom = New-Object System.Text.UTF8Encoding $true
$fixed = 0
foreach ($f in (Get-ChildItem -Path $Target -Recurse -Filter '*.ps1').FullName) {
    $bytes = [System.IO.File]::ReadAllBytes($f) | Select-Object -First 3
    $hasBom = ($bytes.Count -ge 3 -and $bytes[0] -eq 0xEF -and $bytes[1] -eq 0xBB -and $bytes[2] -eq 0xBF)
    if (-not $hasBom) {
        $content = [System.IO.File]::ReadAllText($f, [System.Text.Encoding]::UTF8)
        [System.IO.File]::WriteAllText($f, $content, $utf8Bom)
        $fixed++
    }
}
Write-Host "fixed: $fixed (expect 1)"
```

Expected: `fixed: 1`

- [ ] **Step 3: 验证**

```powershell
$f = "$env:TEMP\mpdev-v2.0.1-test3\scripts\migrate-from-v1.ps1"
$b = [System.IO.File]::ReadAllBytes($f) | Select-Object -First 3
Write-Host "BOM bytes: $($b | ForEach-Object { '0x{0:X2}' -f $_ })"
```

Expected: `BOM bytes: 0xEF 0xBB 0xBF`

- [ ] **Step 4: 清理**

```powershell
Remove-Item -Recurse -Force $env:TEMP\mpdev-v2.0.1-test3
```

---

## Task 11: 验收剧本 #4 — migrate.sh 不带 flag

**Files:** 无修改

- [ ] **Step 1: 准备沙盒 v1 项目**

```bash
cd F:/claude/superdev && rm -rf /tmp/v1-migrate-default && \
  mkdir -p /tmp/v1-migrate-default/.claude/agents /tmp/v1-migrate-default/.claude/commands /tmp/v1-migrate-default/.claude/templates && \
  echo '1.3.0' > /tmp/v1-migrate-default/.claude/.mpdev-version && \
  echo '# code-rev (v1)' > /tmp/v1-migrate-default/.claude/agents/code-reviewer.md && \
  echo '# int-check (v1)' > /tmp/v1-migrate-default/.claude/agents/integration-checker.md && \
  echo '# acc-rev (v1)' > /tmp/v1-migrate-default/.claude/agents/acceptance-reviewer.md && \
  echo '# doc-refr (v1)' > /tmp/v1-migrate-default/.claude/agents/doc-refresher.md && \
  echo '# java agent' > /tmp/v1-migrate-default/.claude/agents/java-impl.md && \
  echo '# architect' > /tmp/v1-migrate-default/.claude/agents/architect.md && \
  echo '# old cmd' > /tmp/v1-migrate-default/.claude/commands/mpdev-fix.md && \
  ls /tmp/v1-migrate-default/.claude/agents/ | sort
```

Expected: 6 个 agent 文件（4 框架 + java-impl + architect）

- [ ] **Step 2: 跑 migrate.sh 不带 flag**

```bash
cd /tmp/v1-migrate-default && bash F:/claude/superdev/mpdev/scripts/migrate-from-v1.sh 2>&1 | tail -25
```

Expected: 末尾输出含 `推荐删 4 个框架 agent` 提示段，**不**含 `已清理 N 个框架 agent`

- [ ] **Step 3: 验证 4 框架 agent 仍在**

```bash
ls /tmp/v1-migrate-default/.claude/agents/ | sort
```

Expected: 6 个文件全保留（code-reviewer / integration-checker / acceptance-reviewer / doc-refresher / java-impl / architect）

- [ ] **Step 4: 清理**

```bash
rm -rf /tmp/v1-migrate-default
```

---

## Task 12: 验收剧本 #5 — migrate.sh `--auto-clean-framework-agents`

**Files:** 无修改

- [ ] **Step 1: 准备沙盒 v1 项目（同 Task 11 但用 autoclean 命名）**

```bash
cd F:/claude/superdev && rm -rf /tmp/v1-migrate-autoclean && \
  mkdir -p /tmp/v1-migrate-autoclean/.claude/agents /tmp/v1-migrate-autoclean/.claude/commands && \
  echo '1.3.0' > /tmp/v1-migrate-autoclean/.claude/.mpdev-version && \
  for a in code-reviewer integration-checker acceptance-reviewer doc-refresher java-impl architect; do
    echo "# $a" > /tmp/v1-migrate-autoclean/.claude/agents/$a.md
  done && \
  echo '# old cmd' > /tmp/v1-migrate-autoclean/.claude/commands/mpdev-fix.md && \
  ls /tmp/v1-migrate-autoclean/.claude/agents/ | wc -l
```

Expected: 6（agent 文件数）

- [ ] **Step 2: 跑 migrate.sh 带 flag**

```bash
cd /tmp/v1-migrate-autoclean && bash F:/claude/superdev/mpdev/scripts/migrate-from-v1.sh --auto-clean-framework-agents 2>&1 | tail -30
```

Expected: 
- 输出含 `--auto-clean-framework-agents: 移除 4 个框架 agent`
- 输出含 `✅ 已清理 4 个框架 agent`
- 末尾"后续"提示含 `✅ 框架 agent 已被 plugin 接管`，**不**含 `推荐删 4 个框架 agent`

- [ ] **Step 3: 验证 4 框架 agent 被删，2 个项目特化 agent 保留**

```bash
ls /tmp/v1-migrate-autoclean/.claude/agents/ | sort
```

Expected: 只剩 `architect.md`、`java-impl.md` 2 个

- [ ] **Step 4: 验证 mpdev-runs / .mpdev-* 等其他项目数据保留（如果有）**

(本沙盒没建这些数据，跳过此验证)

- [ ] **Step 5: 清理**

```bash
rm -rf /tmp/v1-migrate-autoclean
```

---

## Task 13: 验收剧本 #6 — migrate.sh 未知参数

**Files:** 无修改

- [ ] **Step 1: 准备空沙盒（不需 v1 结构）**

```bash
cd F:/claude/superdev && rm -rf /tmp/v1-bogus && mkdir -p /tmp/v1-bogus && cd /tmp/v1-bogus
```

- [ ] **Step 2: 跑带未知 flag**

```bash
cd /tmp/v1-bogus && bash F:/claude/superdev/mpdev/scripts/migrate-from-v1.sh --bogus 2>&1; echo "exit=$?"
```

Expected: 输出含 `❌ 未知参数: --bogus` 且 `exit=1`

- [ ] **Step 3: 跑 --help**

```bash
bash F:/claude/superdev/mpdev/scripts/migrate-from-v1.sh --help 2>&1; echo "exit=$?"
```

Expected: 输出含 `Usage: migrate-from-v1.sh [--auto-clean-framework-agents]` 且 `exit=0`

- [ ] **Step 4: 清理**

```bash
rm -rf /tmp/v1-bogus
```

---

## Task 14: 验收剧本 #7 — migrate.ps1 4 场景（Windows PowerShell）

**Files:** 无修改

- [ ] **Step 1: 准备沙盒 + 跑 .ps1 不带 flag**

在 PowerShell 中：

```powershell
$sb = "$env:TEMP\v1-migrate-ps1"
Remove-Item -Recurse -Force $sb -ErrorAction SilentlyContinue
New-Item -ItemType Directory -Force -Path "$sb\.claude\agents", "$sb\.claude\commands" | Out-Null
'1.3.0' | Set-Content -Path "$sb\.claude\.mpdev-version" -Encoding utf8 -NoNewline
foreach ($a in 'code-reviewer','integration-checker','acceptance-reviewer','doc-refresher','java-impl','architect') {
  "# $a" | Set-Content -Path "$sb\.claude\agents\$a.md" -Encoding utf8
}
'# old' | Set-Content -Path "$sb\.claude\commands\mpdev-fix.md" -Encoding utf8

Push-Location $sb
& powershell -NoProfile -ExecutionPolicy Bypass -File F:\claude\superdev\mpdev\scripts\migrate-from-v1.ps1
Pop-Location

Write-Host "=== After (without flag) ==="
Get-ChildItem "$sb\.claude\agents\" -File | Select-Object -ExpandProperty Name | Sort-Object
```

Expected: 6 个 agent 全部保留

- [ ] **Step 2: 重置沙盒 + 跑 --auto-clean-framework-agents**

```powershell
Remove-Item -Recurse -Force $sb
New-Item -ItemType Directory -Force -Path "$sb\.claude\agents", "$sb\.claude\commands" | Out-Null
'1.3.0' | Set-Content -Path "$sb\.claude\.mpdev-version" -Encoding utf8 -NoNewline
foreach ($a in 'code-reviewer','integration-checker','acceptance-reviewer','doc-refresher','java-impl','architect') {
  "# $a" | Set-Content -Path "$sb\.claude\agents\$a.md" -Encoding utf8
}

Push-Location $sb
& powershell -NoProfile -ExecutionPolicy Bypass -File F:\claude\superdev\mpdev\scripts\migrate-from-v1.ps1 --auto-clean-framework-agents
Pop-Location

Write-Host "=== After (with flag) ==="
Get-ChildItem "$sb\.claude\agents\" -File | Select-Object -ExpandProperty Name | Sort-Object
```

Expected: 只剩 `architect.md`、`java-impl.md`

- [ ] **Step 3: 测试未知 flag 报错**

```powershell
Push-Location $sb
& powershell -NoProfile -ExecutionPolicy Bypass -File F:\claude\superdev\mpdev\scripts\migrate-from-v1.ps1 --bogus
Write-Host "exit code: $LASTEXITCODE (expect 1)"
Pop-Location
```

Expected: 输出含 `❌ 未知参数: --bogus`，`$LASTEXITCODE = 1`

- [ ] **Step 4: 测试 --help**

```powershell
& powershell -NoProfile -ExecutionPolicy Bypass -File F:\claude\superdev\mpdev\scripts\migrate-from-v1.ps1 --help
Write-Host "exit code: $LASTEXITCODE (expect 0)"
```

Expected: 输出含 `Usage: migrate-from-v1.ps1 [--auto-clean-framework-agents]`，`$LASTEXITCODE = 0`

- [ ] **Step 5: 清理**

```powershell
Remove-Item -Recurse -Force $env:TEMP\v1-migrate-ps1
```

---

## Task 15: 综合 git log + 最终自查

**Files:** 无修改

- [ ] **Step 1: 综合 git log**

```bash
cd F:/claude/superdev && git log --oneline 61631e6..HEAD
```

Expected: 看到约 7 个 commit（T1-T7 各一个）：
- feat(install.sh): add BOM auto-fix
- feat(install.ps1): add BOM auto-fix
- feat(migrate.sh): add --auto-clean-framework-agents
- feat(migrate.ps1): add --auto-clean-framework-agents
- release: bump VERSION + manifests to 2.0.1
- docs(changelog): add [2.0.1]
- docs(readme): bump mpdev badge to v2.0.1

- [ ] **Step 2: diff stat**

```bash
cd F:/claude/superdev && git diff --stat 61631e6..HEAD | tail -3
```

Expected: 约 9 文件 / +130 insertions / 极少 deletions

- [ ] **Step 3: 验证 v2.0.1 标识**

```bash
cd F:/claude/superdev && \
  echo "VERSION:" && cat mpdev/VERSION && echo "" && \
  echo "plugin.json:" && grep '"version"' mpdev/.claude-plugin/plugin.json && \
  echo "marketplace.json:" && grep '"version"' mpdev/.claude-plugin/marketplace.json && \
  echo "CHANGELOG:" && head -25 mpdev/CHANGELOG.md | grep "\[2.0.1\]" && \
  echo "README badge:" && grep "mpdev-v2.0.1" README.md
```

Expected: 全部输出 2.0.1

- [ ] **Step 4: 残留 v2.0.0 检查（除 CHANGELOG 历史 + 文档说明外不应有）**

```bash
cd F:/claude/superdev && grep -rn "2.0.0" mpdev/VERSION mpdev/.claude-plugin/ 2>/dev/null && echo "(should be empty)"
```

Expected: 无输出

---

## Self-Review

实施完成后对照 spec [`docs/superpowers/specs/2026-05-18-mpdev-v2.0.1-patches.md`](../specs/2026-05-18-mpdev-v2.0.1-patches.md)：

- [ ] §4 修改清单 9 文件全部覆盖 → T1-T7 覆盖 ✓
- [ ] §5.1 install.sh BOM 自检 → T1 实施 + T8/T9 验证 ✓
- [ ] §5.2 install.ps1 BOM 自检 → T2 实施 + T10 验证 ✓
- [ ] §6.1 migrate.sh --auto-clean → T3 实施 + T11/T12/T13 验证 ✓
- [ ] §6.2 migrate.ps1 --auto-clean → T4 实施 + T14 验证 ✓
- [ ] §7 CHANGELOG 草稿 → T6 实施 ✓
- [ ] §8 7 个验收剧本 → T8-T14 ✓
- [ ] §9 风险 (find -print0) → T1 Step 2 已用 ✓
- [ ] §10 不在范围（明确不做的 4 项）→ 0 task 涉及 ✓
- [ ] §12 发布步骤（git tag + push）→ 留到剧本全过后手动执行，不在本 plan 范围
